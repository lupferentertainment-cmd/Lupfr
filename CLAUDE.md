# Lupfr — Claude Code Instructions

@AGENTS.md

Mandatory order for every task: **read docs → write plan → implement → verify.**
`docs/*.md` is the authoritative spec — read the owning doc(s) before any edit,
and write `tmp/plan.md` (Will NOT Change / Drift Risks / Verification Plan)
before non-trivial changes. Full details in `AGENTS.md` above.
