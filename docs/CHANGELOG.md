# Changelog

All notable project changes are recorded here.

## [Unreleased]

### Fixed

- **Partners-marquee hover-pause dropping mid-hover (logo clicks missed):** the grab-to-spin engine's JS pause listeners lived on the composited, continuously-translating marquee track; Chromium hit-tests a layer moving under a stationary cursor against stale geometry and fires a spurious `mouseleave` ~400ms after enter, un-pausing the row so logos slid out from under the click. Pause listeners moved to the static partners `<section>` (same host the CSS `:hover` fallback always used). New Playwright gate `scripts/verify-partners-marquee.mjs` (`bun run _verify:partners-marquee`, in `run_browser_checks`) locks the full interaction contract: hover-pause holds ≥1.2s, plain logo clicks navigate, drags spin the track with their click suppressed, touch swipes follow the finger. `docs/DESIGN.md` also corrected: drag-click threshold is 12px, not 6px.

### Added

- **Admin portal expansion (analytics + contacts + telemetry):** In-portal **Vercel Web Analytics** charts via `GET /admin/api/analytics` (`LUPFR_VERCEL_*` env; real series only). Optional **Supabase** dual-write from `/api/phone-list` + admin contacts table/CSV (`GET /admin/api/contacts`, `/admin/api/contacts/export`). Consent-gated **`POST /api/telemetry`** + admin impressions/clicks panel (`GET /admin/api/telemetry`). Sheet CTA/iframe still via `ADMIN_CONTACTS_SHEET_URL`. Password admin unchanged; no CMS.

- **Operator admin portal (MVP):** password-gated shell at **`admin.lupfr.com`** (host rewrite) and **`/admin`**. Shared env credential (`ADMIN_USERNAME` default `will@lupfr.com`, `ADMIN_PASSWORD` + `ADMIN_SESSION_SECRET` ≥32 UTF-8 bytes), HMAC session cookie `lupfr_admin_session` (APIs at `/admin/api/*` so `Path=/admin` works on apex; `Path=/` on admin host). Auth in `app/admin/(authed)/layout.tsx` — proxy is rewrite/SEO only. Analytics-first dashboard (Vercel Analytics deep-link + note of in-app `DeferredAnalytics`); **Recharts** content charts (upcoming/past events, artist genres — not fake traffic); CSV downloads via `/admin/api/export/*`; contacts Sheet CTA via optional `ADMIN_CONTACTS_SHEET_URL`; ops links; no CMS/DB/Stripe. Robots Disallow + sitemap exclude + noindex. Marketing chrome suppressed on `admin.*` hosts. Playwright gate `scripts/verify-admin.mjs` (`bun run _verify:admin`) covers login + dashboard + export auth in local/GHA browser checks. Docs flipped in the same change.

### Changed

- **July 24 website asset refresh (owner zip):** refreshed curated WebPs from `LUPFR WEBSITE updates_july24` — artists, event posters, brand cards + SEA//SIDE gallery 1–6, team portraits, corporate partners (incl. Partiful/SEA//SIDE/Umbrella), Partiful announcement tile, and services imagery. Path-stable YAML. Optimize card re-encode can step quality/width further so dense posters (e.g. marina_music) stay ≤450KB. Deferred: VIEW DECK slides, raw hero MP4, uploads/screenshots.

- **Partiful team-band art (owner request 2026-07-23):** `/images/partiful-announcement.webp` no longer uses the fromclay event flyer — swapped to a Partiful P mark on a dark gold stage so the “Backed by Partiful” band reads as partnership, not a show poster. `docs/DESIGN.md` updated in the same change.

- **Partiful visibility + HD mobile hero (owner request 2026-07-23):** Partiful partner marks punch out baked black backgrounds (transparent freeform) and scale up in the strip; mobile hero uses a sharper yacht still (`hero-poster-yacht-mobile.webp` ~1600px) then fades in the same full-quality yacht MP4 after `window` load + idle (skipped for `prefers-reduced-motion` so the mobile perf gate stays poster-only). Team “Backed by Partiful” band unchanged. `docs/DESIGN.md` updated in the same change.

- **Softer event-card depth (owner request 2026-07-23):** `.event-card-depth` resting/hover shadows use restrained black elevation instead of light/`--foreground` mixes that read as a white halo around home Past/Upcoming and `/events` cards. `docs/DESIGN.md` updated in the same change.

- **Freeform Corporate Partners marquee (owner request 2026-07-23):** the under-hero logo strip drops rectangular bordered/card tiles — logos and pending name labels render freeform on the page background (height-capped marks, muted→full opacity on hover, wider gaps; no skeleton tile behind marks). Section marked `data-partners-chrome="freeform"`. Enforced by Playwright `verify-will-home` (computed shell bg/border/shadow + no `.partner-logo-chip`), RTL partners/will-home tests, and `look-and-feel` / `test-suite-gate` contracts. Grab-to-spin, eyebrow, and roster unchanged. `docs/DESIGN.md` + `docs/TESTING.md` updated in the same change.

### Fixed

- **Vercel Playwright OS libs (2026-07-22):** Vercel `buildCommand` sets `LUPFR_SKIP_BROWSER_CHECK=1` and drops Playwright from `installCommand` — the sandbox cannot install Chromium system libraries (`libnspr4.so`), which blocked Production after Vitest/routes went green. Local/GHA still run the full browser gate. `docs/DEPLOYMENT.md` + `docs/TESTING.md` updated in the same change.

- **Vercel verify-routes /dev/fd (2026-07-22):** `scripts/verify-routes.sh` no longer uses bash process substitution (`< <(...)`) for link extraction — Vercel’s build sandbox lacks `/dev/fd`, which aborted the crawl after `/` and failed Production. Uses temp files under the existing mktemp dir instead.

- **Cookie consent honors cookie fallback (2026-07-22):** `getCookieConsentAccepted()` now returns true when the `lupfr_cookie_consent` cookie is set even if `localStorage` writes failed (private mode / quota), matching `acceptCookieConsent`. Cookie-consent RTL tests expire that cookie with `Max-Age=0` (happy-dom ignores `document.cookie = ""`). Unblocks Vercel Vitest (`phone-list-preferences` contact-list cookie case) and Production deploys that ship Partiful partners.

- **Vercel Vitest localStorage spies (2026-07-22):** phone-list preference and cookie-consent tests spy `localStorage` instance methods instead of `Storage.prototype`, so the full Vercel build gate does not fail when happy-dom’s storage object is not on that prototype chain (unblocked Production deploys that ship Partiful partners).

### Added

- **Full local test suite on Vercel (owner request 2026-07-22):** Vercel build runs Vitest coverage + route/asset crawls + `_build` before deploy. Playwright browser crawls remain local/GHA-only (see Fixed: Vercel Playwright OS libs). `docs/TESTING.md` + `docs/DEPLOYMENT.md` updated in the same change.

- **90% Vitest coverage + Will Playwright edge gate (2026-07-22):** raised global coverage thresholds to **90%** (statements/branches/functions/lines); excluded retired `components/seaside/**` and generated JSON from the denominator; added RTL/edge-case tests (partners rect tiles, past compact, services Explore/Learn more, ASTRD, Partiful team band, drive-gallery fetch, data-normalization branches) and Playwright `scripts/verify-will-home.mjs` wired into `bun run test` browser checks. Past home cards shrink further to ~180–196px (`data-compact`). `docs/TESTING.md` + `docs/DESIGN.md` updated in the same change.

- **Home Services redesign parity (Will screenshot 2026-07-22):** the home `#services` header CTA is now **"Explore all services →"** (was "View all services →"), and each home card drops its icon chip and dash-led feature-bullet list in favor of a gold **"Learn more →"** text link at the card's bottom — features/icons stay on the `/services` overview and `/services/<slug>` detail pages, which are unchanged. Numeral, photo header, title, description, desktop tilt/hover motion, and the mobile static card path are unchanged. `tests/unit/mobile-services-static.test.ts` updated in the same change; `docs/DESIGN.md` updated in the same change.

- **Event card brand box tags (owner request 2026-07-22):** home event cards' brand-wordmark row is now a small bordered mono-uppercase box (brand-accent border/text + 7px filled accent dot) around the `BrandSlashText` `brandTag`, matching the Our Brands tag-pill style Will flagged in his screenshot; the date keeps its place on the right. Past (compact) cards get a smaller box. `components/events-directory.tsx`'s brand tag picked up the same box+dot for consistency. No YAML/data changes. `docs/DESIGN.md` updated in the same change.

- **Owner website updates (Will email 2026-07-21):** rectangular corporate-partner logo tiles (no circles); roster adds Partiful + FredEx Entertainment, updates transparent SEA//SIDE + larger Nazarian Law, removes Venn Social + Soundcheck; Brands gains “View all brands →” to `/brands`; fromclay+thatfranco moves to Paris 75 / 8 PM–2 AM / Partiful tickets with updated lineup; BAL MASQUE gets Partiful ticket link; home event cards use brand-accent borders/wordmarks and Past cards shrink; ASTRD joins the artist roster; home roster sorts A–Z with featured six gold+bold; Team adds a Partiful partnership announcement band; Services header links to `/services`. Hero stays desktop-video / mobile-poster (perf). `docs/DESIGN.md` updated in the same change.

- **Removed homepage “Five brands” tagline (owner request 2026-07-19):** the Our Brands section drops the “Five brands, one company — LUPFR Entertainment spans live music events and private corporate programming.” lead sentence and its now-unused `TextReveal` import (`components/brands.tsx`). The “The Portfolio · Five Series” kicker, “Our Brands” heading, and all five brand cards are unchanged.

- **BAL MASQUE venue set to The Hibernia (owner request 2026-07-19):** `data/events.yml` gives the Aug 29 San Francisco masquerade its now-known venue — `location: The Hibernia, San Francisco, CA` and a description that drops the “venue reveal to come” wording (lineup reveal still pending). Generated `events.json` regenerated in the same change.

- **Clickable artist roster deep links (owner request 2026-07-19):** the all-name roster under Featured Artists is now interactive, porting the comp's clickable name tags — each name links to `/artists?artist=<slug>`, where the directory highlights the matching card (accent ring + `aria-current`) and auto-scrolls it into view (reduced-motion aware). Visual treatment (gold/muted names, no chips) unchanged; hover/focus adds an accent underline. `docs/DESIGN.md` updated in the same change.

- **Events carousel free scroll + arrow controls (owner request 2026-07-19):** the home Upcoming/Past event rows drop the hard snap rebound for free momentum drag (`dragFree`) and gain Lupfr-styled circular prev/next arrow buttons overlaid on both sides of each row — gallery-lightbox chip styling with gold hover, paging by the visible group (`slidesToScroll: "auto"`), fading out at the row ends, and unmounting when every card already fits. `docs/DESIGN.md` updated in the same change.

- **Corporate footer redesign + event-detail upgrades + global section rhythm (owner requests 2026-07-19):** the footer adopts the Eria-Website treatment the owner likes, kept corporate for LUPFR — spelled-out **LUPFR** wordmark in the condensed display face via `GoldShineText` over a short gold hairline (replacing the brand-column logo image; the back-to-top logo control stays), vertical gold rules splitting the brand/Company/newsletter columns on desktop (`lg:divide-x lg:divide-accent/15`), short gold underline accents under the column eyebrows, and a new "Trusted by our partners" **logos band** (all `getPartners()` marks at `h-7/8` with mono `.partner-logo*` treatment and dark-image swap, linked `_blank`+`noopener`, label-only partners as mono text) between the grid and the bottom bar. Event detail pages gain gold spec-row hairlines, a Google-Maps external link on the LOCATION row, a brand-page link on the brand kicker, and a bottom prev/next event nav with a centered All Events link (services-detail parity). Home-section vertical spacing is now one global token — `--lupfr-section-pad` (`clamp(2.5rem, 1.75rem + 2.5vw, 4rem)`) applied via `.lupfr-section-pad` on Brands/Events/Services/Team/Contact and as `pb-(--lupfr-section-pad)` on the tight-top About/Artists — cutting desktop neighbor gaps from ~232px to ~128px. `docs/DESIGN.md` updated in the same change.

- **Visible Corporate Partners label (owner request 2026-07-17):** restored a restrained centered “Corporate Partners” eyebrow above the full-bleed logo marquee, using the shared small mono-uppercase gold kicker treatment. Partner order, links, circular chips, grab-to-spin behavior, inherited page background, and accessible section label remain unchanged; `docs/DESIGN.md` and `docs/ARCHITECTURE.md` now supersede the July 11 no-visible-text rule.

- **Hero legibility + LUPFR return-to-top (owner request 2026-07-17):** the existing metallic mono “Redefining the Music Experience” line now has a stable video-safe contrast edge in both themes, and the Scroll cue uses high-contrast white/shadow with gold hover/focus while preserving layout and motion. The footer's accessible Back to top button now includes the LUPFR wordmark in its compact pill.

- **Restrained 3D event-card system (owner request 2026-07-17):** home and `/events` cards share CSS-only layered depth and a subtle hover/focus sheen; directory cards gain a fine-pointer perspective lift. Reduced motion disables the sheen, touch keeps the static baseline, and no shader/effect dependency was added.

- **Navbar active-section Playwright regression (owner request 2026-07-17):** fixed home hash navigation leaving the previous link (for example Brands after clicking Events) gold/underlined when the browser lands the target on a fractional pixel just below the nominal 120px scroll-spy line. The spy now samples one CSS pixel below the header offset, and the real-Chromium gate asserts that exactly the clicked desktop/mobile link owns both `aria-current` and the full-width rendered underline after settling.

- **BAL MASQUE poster + Partiful About proof (owner request 2026-07-17):** assigned the delivered portrait poster to BAL MASQUE (`public/events/bal_masque.webp` → `data/events.yml`) so its home, directory, and detail surfaces no longer show the generic Coming soon tile. The About story now names LUPFR's exclusive partnership with Partiful and connects Partiful's community platform to LUPFR's on-the-ground production. Focused tests pin the event-to-poster mapping and partnership copy; `docs/DESIGN.md` updated in the same change.

- **Corporate polish pass — depth, shimmer, animated text, icon density (owner request 2026-07-17):** extends the existing polish primitives to the surfaces that lacked them, no palette/type/structure changes. `/events` directory cards (`components/events-directory.tsx`) gain the shared gold `ShimmerImage` poster loading (locked into `tests/unit/image-shimmer-coverage.test.ts`), a staggered rise-in card reveal (`CardReveal`, opacity/transform only, once), desktop hover lift + depth shadow + fine-grained poster zoom (`motion-safe` gated), and icons — `CalendarDays`/`History` on the Upcoming/Past status pills, `MapPin` in the city overlay pill, `CalendarDays` beside the card date. Event detail's LINEUP spec row gains a `Mic2` icon matching its Location/When siblings, and the right spec column rises in via `ScrollReveal`. Service detail pages now render the service's own Lucide icon as a bordered chip beside the kicker (previously dropped on detail routes) and reveal their two-column grid + per-word description (`TextReveal`); brand detail pages and the `/brands` overview rows get the same `ScrollReveal`/`TextReveal` entrance treatment. Home brand cards add a subtle hover lift + shadow (still no tilt — informational cards). The Featured Artists lead paragraph moves onto `TextReveal`, matching the other section leads. All motion stays opacity/transform-only and reduced-motion-safe; mobile static branches and locked theme/class contracts untouched. `docs/DESIGN.md` updated in the same change.

- **Maison Noir label-only partner chip (owner request 2026-07-17, comp parity):** the comp's partner strip lists `MAISON NOIR` with `logo: null`; the site had skipped it pending a logo asset. `data/partners.yml` now carries a name-only Maison Noir row and the marquee renders it as the comp's text-label fallback — the same circular `partner-logo-chip` with the partner name in centered mono uppercase instead of a logo (`PartnerLabelChip` in `components/partners-strip.tsx`), unlinked until a destination exists. `PartnerItem.image`/`url` are now optional (`lib/data/partners.ts`; normalization and `imageClassName` only apply to logo rows). The owner has the logo asset but hasn't delivered it — when it lands, add `image: /corporate_partners/maison_noir.webp` to the row. Partner list/integrity/strip tests updated (image/url checks scoped to rows that define them; new label-chip contract: name text visible, no `img`, no dead anchor); `docs/DESIGN.md` updated in the same change.

- **Grab-to-spin partners marquee (owner request 2026-07-17):** the corporate-partners marquee under the hero can now be grabbed and flung. After hydration, `useMarqueeSpin` (`components/partners-strip.tsx`) marks the track with `data-spin`, disables the CSS keyframe (kept as the no-JS fallback), and drives the same 50s/half-loop auto-advance from a rAF loop seeded from the keyframe's current transform; pointer/touch drags spin the row directly (`cursor: grab`/`grabbing`, `touch-action: pan-y` preserves vertical page scroll) and release flings with momentum that decays back into auto-scroll. The hover/focus pause is replicated in JS, a drag past 6px suppresses the next click (capture phase, one-shot) so flings never accidentally open a partner site while plain clicks still navigate, and `prefers-reduced-motion` users keep the static `overflow-x: auto` fallback untouched. New `[data-spin]`/`[data-dragging]` rules in `app/globals.css`; drag-contract tests added to `tests/components/partners-strip.behavior.test.tsx`; `docs/DESIGN.md` + `docs/TESTING.md` updated in the same change.

- **All Events page + comp event-detail split (owner restructure 2026-07-17, ported from the comp's #/events and #/event pages):** new `/events` directory (`app/events/page.tsx` + client `components/events-directory.tsx`) — kicker "Upcoming & Past", gold-shine "All Events" h1, status pills (All/Upcoming/Past) and brand-wordmark filter pills, Upcoming card grid ("IN N DAYS"/TODAY/TOMORROW/DATE TBD + city pills, brand-accent mono tag, short date, condensed uppercase title, gold lineup line) and reduced-opacity Past grid ("N DAYS AGO"); all cards link to detail routes; day math uses `todayDateISOInEventTZ`. The event detail page moves to the comp's split layout: sticky `aspect-[4/5]` cover poster left (striped "Coming soon" tile when no poster), brand-accent kicker + condensed h1 + LINEUP/LOCATION/WHEN hairline spec rows + full-width gold ticket pill right, with description/gallery/content-links/share below (mono section labels); the old single-column `max-w-4xl` card and the now-unused `components/event-detail-hero-image.tsx` are removed. The home Events header's "View all events →" link now targets `/events`. `data/events.yml` gains comp `brandTag`/`city` on six existing events and the comp's five announced events (BAL MASQUE, Zusebi 001–003, SEA//SIDE 002; `ticketStatus: tbd`, no posters). Sitemap picks `/events` up automatically; asset crawl + new `events-directory` behavior test + `data-integrity` brandTag↔brands check added. `docs/ARCHITECTURE.md`, `docs/DESIGN.md`, `docs/TESTING.md` updated in the same change.


- **Artist roster — GasMoney, Juggan, trillbot (owner photo delivery 2026-07-17):** the three names from the morning featured-track delivery that had no roster row now have full entries in `data/artists.yml` (ids 15–17, appended after Midfield Avenue), unblocked by owner-delivered photos (`public/artists/gasmoney.webp` / `juggan.webp` / `trillbot.webp`, WebP-optimized through the standard pipeline). Each entry carries its already-delivered featured track (`?si=` stripped per the data-file rule) plus a Spotify profile link resolved from the delivered track's own artist credits (Juggan disambiguated from the 3-artist credit on "Can't Move On"; trillbot's "VENTE" credit also matches thatfranco's rostered artist id). Genres and bios are interim, evidence-based placeholders (GasMoney: Rock — live-band photo + the GAS MONEY event; trillbot: House — DJ photo + the thatfranco collab; Juggan: Electronic umbrella) pending owner copy, same deliberate-placeholder convention as prior owner change-sets. `tests/unit/artists-list.test.ts` (roster order + new delivery pins) updated in the same change.

- **Per-brand detail pages (owner request 2026-07-17, ported from the comp):** every brand in the portfolio is now a clickable page at `/brands/<key>` (`app/brands/[slug]/page.tsx`, statically generated from `data/brands.yml` with `dynamicParams` off so unknown slugs 404), plus a new overview index at `/brands` (`app/brands/page.tsx`) porting the comp's five alternating `lp-brand-row` blocks — an accent-tinted photo panel (tag pill, condensed wordmark, format line) beside a description column showing seaside's 3-up gallery preview, a centered "Coming soon" block for `comingSoon` brands (highrise, soundcheck, new optional YAML fields), or two-column Upcoming/Past-highlights event lists (`event.brandTag === brand.title`, via the existing `getUpcomingEvents`/`getPastEvents` helpers) with the comp's empty-state copy. `lib/data/brands.ts` gains `brandPath()`, `getBrandBySlug()`, and `brandPlainTitle()` (collapses "SEA//SIDE" → "SEA SIDE" for `<title>`/aria text) alongside the new `comingSoon`/`gallery` `BrandItem` fields. Home `components/brands.tsx` cards now link their wordmark to the detail page and gained a "Learn more →" row; brand routes are added to `app/sitemap.ts` and the asset-crawl route builder. `tests/unit/brand-routes.test.ts` (new) and the extended `tests/components/brands.behavior.test.tsx` lock the route/link contract; `docs/DESIGN.md` and `docs/ARCHITECTURE.md` updated in the same change.

- **Corporate type system, phase 34 — spaced "//" dividers (owner request 2026-07-17):** `BrandSlashText` now renders exactly one space on each side of every `//` divider ("SEA // SIDE" instead of "SEA//SIDE"), normalizing at render time — split parts are trimmed, so already-spaced source strings (the hero corner readout's coordinates/EST lines) don't double up and data files keep their tight canonical form (`data/brands.yml` still says `SEA//SIDE`). Applies to every `BrandSlashText` call site: Brands cards, About roll call, event cards and the event detail `h1`, `/services` related-brand pills, and the hero readout. `docs/DESIGN.md` and the brand-slash-text/brands behavior tests updated in the same change.

- **Per-service detail pages (owner request 2026-07-17):** every service is now a clickable page at `/services/<service-slug>` (`app/services/[slug]/page.tsx`, statically generated from `data/services.yml` with `dynamicParams` off so unknown slugs 404). Each page reuses the overview row's comp design language (Service NN kicker, condensed-uppercase gold `h1`, 4:3 photo with numeral, dash-led features, related-brand pills) plus a "Book This Service" CTA to `/contact`, prev/next service nav, and an All Services back link. `servicePath()` in `lib/data/services.ts` now returns the dedicated route (was `/services#<anchor>`), so home Services cards and the new overview-row title / "Learn more →" links all land on the detail pages; service routes are added to `app/sitemap.ts` and the asset-crawl route builder. `docs/ARCHITECTURE.md`, `docs/DESIGN.md`, and the service route/behavior tests updated in the same change.

- **About restructure — "Built From the Ground Up" merged About/News section (owner restructure 2026-07-17, ported exactly from the comp's `#news` section):** `components/about.tsx` now carries the comp's copy verbatim — kicker "About LUPFR", heading "Built From the Ground Up", two story paragraphs (2025 founding; the five-brand roll call with per-brand accent-colored `BrandSlashText` slashes; LA + SF production), and the gold-left-border founder quote signed "— Will Lupfer, Founder & CEO" — replacing the old "Redefining the Music Experience" copy, its third paragraph, the byline/address block, and the three Curation/Expertise/Quality value cards. The right column is the featured press card (`getPress()[0]`), so the standalone Press/News carousel section is retired: `components/press.tsx` deleted, the `id="news"` deferred slot and nav "News" link removed, `DEFERRED_SECTION_IDS` shrinks to about/team/contact. The SF Post article title in `data/press.yml` is shortened to the comp's exact "From Energy Projects to Yacht Parties: How LUPFR Is Redefining the Scene" (same URL/image/date). The street address moves to the footer as the comp has it ("87 N Raymond, Floor 6, Pasadena, CA", replacing "Based in SF & LA, California"). `docs/DESIGN.md`, `docs/ARCHITECTURE.md`, `docs/TESTING.md`, and the home-performance / hash-scroll / look-and-feel guardrails updated in the same change.

- **Partners marquee clickability regression lock (owner request 2026-07-17):** confirmed every logo in the corporate-partners marquee is a clickable external link — the redesign's `components/partners-strip.tsx` carries the same `href={url}` wiring as the last tagged release (`v1.19.3`, then rendered inside `services.tsx`), with all 11 `data/partners.yml` rows holding a destination URL (Soundcheck gained one post-tag). Added component tests pinning the contract (one anchor per partner with the exact YAML `url`, `target="_blank"` + `rel="noopener"`, duplicate marquee copy `aria-hidden`/`inert`); no runtime code change was needed. `docs/TESTING.md` updated in the same change.

- **Services restructure copy + related-brand pills (owner restructure 2026-07-17, ported from the comp's services page):** `data/services.yml` adopts the comp's updated descriptions for all six services, renames "Venue Programming" → "Event Programming" (anchor `/services#event-programming`; the `venue-programming.webp` thumbnail filename is unchanged, matching the comp's own asset naming), and gains an optional `relatedBrands` brand-key list resolved against `data/brands.yml` at build time (`lib/data/services.ts`). `/services` rows now render the comp's related-brand chips — bordered uppercase mono pills in each brand's accent with the `BrandSlashText` divider — after the feature list; home Services cards are unchanged. `docs/DESIGN.md` updated in the same change.

- **Corporate type system, phase 33 — brand-accent "//" dividers on Our Brands (owner request 2026-07-17, ported from the comp):** each brand card's wordmark divider (SEA//SIDE, HIGH//RISE, SOUND//CHECK, IN//SIDE, OUT//SIDE) is now colored with that brand's `accent` hex — matching its tag pill and status dot — instead of the shared gold, reproducing the comp's per-brand `//` treatment. `BrandSlashText` gains an optional `color` prop (inline override; skew and gold default unchanged); every other call site (event cards, event detail `h1`, hero corner readout) stays gold. `docs/DESIGN.md` and the brands/brand-slash-text behavior tests updated in the same change.

- **Corporate type system, phase 32 — section grid alignment (owner request 2026-07-17, reversing phase 31's wide-artists exception same-day):** every home section (and the footer) now sits on the comp's uniform grid — shared `max-w-[1400px]` centered wrapper and shared `px-4 sm:px-6 lg:px-12` gutter (comp: `max-width:1400px` + 48px desktop side padding). The Featured Artists section returns from its 1760px wrapper / `xl:gap-16` gutters to the standard 1400px wrapper and 24px desktop grid gap, and About/Team/Press/Contact/Careers/Footer gain the missing `lg:px-12` step so all section content edges align horizontally (they previously drifted 24px against Brands/Events/Services at sub-cap viewports). Vertical rhythm, mobile/tablet gutters, full-bleed hero/partners surfaces, and narrower intentional caps (careers, gallery, legal, event detail) are unchanged. `docs/DESIGN.md` and `tests/unit/event-card-shape.test.ts` updated in the same change.

- **Featured-track permalinks, owner delivery 2026-07-17:** applied the owner's artist → track list to `data/artists.yml`. New `featuredTrack` embeds for Admiral, Alex Rayne, MALEK (owner sent an `on.soundcloud.com` short link, stored as the resolved canonical `soundcloud.com/user-106497389/mainstage-mix` page per the data-file rule), Midfield Avenue, and Nick Rosen; owner-requested track swaps for BAUM (`baum-live-brackish-petaluma`), Where's West? (new Spotify track), and Zusebi (new Spotify track); Auguste, Devvy Dub, fromclay, HLWA, LUPFR, and thatfranco already pointed at the delivered tracks. Three delivered names — GasMoney, Juggan, trillbot — have no artist entry in `data/artists.yml` and were deliberately not invented (no bio/genre/photo delivered); flagged back to the owner. `tests/unit/artists-list.test.ts` pins the delivery in the same change.

- **Our Brands reference geometry (owner audit 2026-07-17):** corrected the last screenshot-confirmed restructure drift in `components/brands.tsx`: cards now use the comp's 460px/20px phone geometry and compact ~340px desktop row with 28×24px padding, while grid gutters switch from 20px on phones to 32px on desktop. Content, photography, links, theme behavior, and the documented Services/Artists product differences are unchanged.
- **Sitewide skeleton shimmer completion (owner request 2026-07-17):** added the existing delayed gold image shimmer and decode fade to the remaining live raster surfaces: home Services, `/services`, Our Brands backgrounds, and Drive-backed gallery image tiles. A thin shared `ShimmerImage` wrapper reuses `SkeletonShimmerLayer` and the existing global CSS; image sources, loading priority, layouts, videos/iframes, dormant blog routes, and the standalone Seaside microsite are unchanged. Regression coverage prevents those four call sites from bypassing the shared loading treatment.
- **Corporate type system, phase 31 — Featured Artists desktop spacing (owner request 2026-07-17):** widened only the home Featured Artists wrapper from 1400px to 1760px and increased its extra-large card gutters from 24px to 64px, matching the supplied four-card reference composition. The shared `/artists` directory keeps its existing 1400px wrapper and compact grid spacing; card content, interactions, and phone/tablet layout are unchanged. `docs/DESIGN.md` and `tests/unit/event-card-shape.test.ts` updated in the same change.
- **Corporate type system, phase 29 — hero drone video (owner decision 2026-07-17, reversing phase 21 same-day):** the desktop hero now plays the comp's drone/aerial yacht footage (`/hero/hero_yacht_001.mp4`, re-encoded from the comp's 15.5MB `assets/hero/yacht-video-001.mp4` source down to 1.57MB, under the existing ≤1.6MB desktop download budget); `HERO_VIDEO_DARK`/`HERO_VIDEO_LIGHT` in `components/hero-desktop.tsx` updated, the prior `hero_event_eria_marina.mp4` left on disk for easy revert. Posters are a matched still from the same clip (`hero-poster-yacht.webp` 1920×1080 ~106KB desktop, `hero-poster-yacht-mobile.webp` 810×1080 3:4 crop ~46KB — both smaller than the ERIA posters they replace in `components/hero-shared.tsx`; one frame serves both themes, same precedent as the video) so desktop fallback and the videoless mobile hero show the same scene as the clip; prior poster files also left on disk. `tests/unit/hero-video-performance.test.ts` updated in the same commit.
- **Corporate type system, phase 28 — Featured Artists static grid (owner redesign 2026-07-17, ported from the comp):** the Artists section renders as a static `grid-cols-1 sm:grid-cols-2 lg:grid-cols-4` grid showing every artist at once, replacing the Embla carousel (arrows, dots, and slide chunking removed) — matching the comp's all-visible 4-across layout. Card internals (photo, genre, name, socials, featured-track embed) unchanged. `tests/unit/event-card-shape.test.ts` (grid guard replaces the carousel-slide guard) and `tests/unit/home-performance.test.ts` updated in the same commit.
- **Corporate type system, phase 27 — Services thumbnail height (owner redesign 2026-07-17, ported from the comp):** the home Services card photo header grows from `h-[120px]` to `h-[180px] sm:h-[220px]`, matching the comp's taller top-third photo proportion (both `ServiceCardStaticBody` and `ServiceCardMotionBody` render paths).
- **Corporate type system, phase 26 — Events card density (owner redesign 2026-07-17, ported from the comp):** the Events carousel card narrows from `lg:basis-[min(480px,33%)]` (≈3-up) to the comp-exact fixed `md:basis-[300px]` square-poster card (≈5-up on wide desktop; phones `basis-[min(300px,84vw)]`), landed in two steps alongside the parallel Events comp-parity rework; the uniform-card-height contract is unchanged. `tests/unit/look-and-feel.test.ts` and `tests/unit/event-card-shape.test.ts` updated in the same change.
- **Dedicated Artists directory (restructure comp parity):** added `/artists` with canonical metadata, the shared artist-card grid, Featured/A–Z ordering, and an All Genres filter; the home Featured Artists header now links to the directory. Covered by `tests/unit/artist-routes.test.ts`; `docs/DESIGN.md` and `docs/ARCHITECTURE.md` updated in the same change.
- **Services overview + card navigation (owner request 2026-07-17):** added a data-driven `/services` page matching the supplied restructure HTML's alternating image/text rows, service numerals, condensed headings, descriptions, and feature lists. Every home service card now links to its matching `/services#<service-slug>` row on mobile, desktop, and keyboard navigation; no duplicated per-service page components or invented related-brand content. URL helpers, accessible links, and route coverage are locked by `tests/unit/service-routes.test.ts`, `tests/components/services.behavior.test.tsx`, and the asset crawl; `docs/DESIGN.md` and `docs/ARCHITECTURE.md` updated in the same change.
- **Corporate type system, phase 25 — Team location/role badges (owner redesign 2026-07-17, reversing phase 12's "not ported" call on direct owner request):** each team card now renders small bordered uppercase mono pills (`member.badges` — union of the existing `teams` filter array and city codes parsed from `location`) below the name/title, matching the comp's proportions. `tests/components/team-section.behavior.test.tsx` and `docs/DESIGN.md` updated in the same commit.
- **Corporate type system, phase 24 — Brands photography (owner redesign 2026-07-17, reversing phase 6's "no new photography" call on direct owner request):** each Brands card gets a full-bleed background photo (`image` in `data/brands.yml`, ported from the comp's `assets/brands/*-bg.jpg`) with a dark gradient scrim, matching the comp's photo-rich card treatment (was flat `bg-card`). `brands` added to `optimize-public-raster.mjs`'s card byte-budget dirs. `tests/unit/data-integrity.test.ts` and `docs/DESIGN.md` updated in the same commit.
- **Corporate type system, phase 23 — base surface palette retint (owner directive, 2026-07-17):** the site's base neutral tokens (`--background`, `--foreground`, `--card`, `--card-foreground`, `--popover`, `--popover-foreground`, `--secondary`, `--muted-foreground`, `--border`, both `:root` and `.dark`) moved from a cool-hued palette to the comp's own warm palette (dark: brownish-black `#0b0a08`/`#141210`; light: warm cream `#faf7f0`/`#ffffff`), taken directly from the comp's `themes.A`/`themes.B` objects. `--border` now uses a translucent overlay (matches the comp's own technique) instead of an opaque color. Owner explicitly and repeatedly directed abandoning the prior "cool off-white / pure black ink" palette (previously locked in `tests/unit/globals-gold-theme.test.ts`) in favor of the comp's exact values — the guardrail test is updated to the new canonical values in the same commit, not silently dropped. Gold tokens, fonts, card shapes, and section content are unchanged. `docs/DESIGN.md` updated in the same commit.
- **Corporate type system, phase 22 — hero wordmark condensed/uppercase (owner redesign 2026-07-17):** the hero's own "LUPFR" / "Entertainment" wordmark moved from `font-serif font-bold tracking-tighter` to `font-condensed font-extrabold tracking-normal`, matching the comp's literal hero markup (`titleFont: 'Barlow Condensed'`, `titleWeight: 800`) — found by opening the comp HTML file directly in a browser and comparing screenshots against the live site side by side, after the owner flagged the hero as visibly different. "LUPFR" keeps its gold-shine gradient (matches the comp, which is also gold-gradient text-clipped). "Entertainment" drops its gold gradient for plain uppercase foreground text, matching the comp exactly; the now-fully-dead `.hero-entertainment-text`/`.hero-entertainment-gradient` classes and their `--entertainment-line-*` tokens were removed, along with the vestigial always-static `shinePosition` prop-drilling that only existed to drive that gradient's `background-position`. `components/hero-desktop.tsx`, `components/hero-shared.tsx`, `app/globals.css`, `docs/DESIGN.md`, and `tests/unit/look-and-feel.test.ts` updated in the same commit.
- **Corporate type system, phase 21 — hero video reviewed, no gap (owner redesign 2026-07-16):** compared the comp's `assets/hero/yacht-video-001.mp4` (generic, unbranded stock-style yacht-party footage, 1920x1080, ~21s) against the live desktop hero video (`hero_event_eria_marina.mp4` — real ERIA Marina event footage, deliberately chosen and byte-optimized in an earlier owner change-set). Owner confirmed (2026-07-17) to keep the current video — the comp's clip has no clear connection to a real LUPFR event and isn't worth trading for authentic branded footage. No code changes.
- **Corporate type system, phase 20 — Featured Artists roster completion (owner redesign 2026-07-16):** added MALEK (House, SoundCloud profile), Alex Rayne (Electronic), and Midfield Avenue (Indie Rock, Spotify) to `data/artists.yml` — the 3 artists photographed for the redesign but explicitly deferred since phase 2 pending real bio/genre/social copy. That copy was found embedded in the comp itself (`image-slot.js`'s inline artist-data array) rather than fabricated: genre, bio text, and social links are taken verbatim. Alex Rayne's `spotifyUrl` in the comp is a `'#'` placeholder, so no social link was added for that artist. Photos WebP-optimized through `optimize-public-raster.mjs` (one re-encode, `midfield_avenue.webp`, to stay under the card byte budget). `docs/DESIGN.md` and `tests/unit/artists-list.test.ts` (roster order + new-artist data contract) updated in the same commit.
- **Corporate type system, phase 19 — main h1/h2 condensed/uppercase heading migration finish (owner redesign 2026-07-16, ported from the comp):** `main h1, main h2` in `app/globals.css` moves from `font-serif font-bold tracking-tighter` (Playfair Display) to `font-condensed uppercase font-extrabold tracking-normal` (Barlow Condensed), matching the comp's bound theme tokens (`titleFont: 'Barlow Condensed'`, `titleTransform: 'uppercase'`, `titleWeight: 800`, confirmed against the comp's `image-slot.js` inline script and its `brands.png` screenshot). This repaints the homepage section `<h2>`s that rely on the bare cascade (About, Team, Artists, Events, Contact, Press, Brands) — `h1`/`h2` elements elsewhere with their own explicit `font-serif` utility class (legal pages, blog, gallery lightbox, 404, global-error, phone-list-popup, nav mobile links) are unaffected since Tailwind class specificity wins over the element-selector rule; the hero `h1` (`hero-title-lupfr`, gold-shine) remains its documented exception. Also removed the dead, zero-usage `.lupfr-section-title`/`.lupfr-subsection-title` classes (superseded by this rule and `.lupfr-heading-sub`, never referenced by any component). `docs/DESIGN.md` and `tests/unit/look-and-feel.test.ts` updated in the same commit.
- **Corporate type system, phase 13 — Partners/Contact/Footer reviewed, no gap (owner redesign 2026-07-16):** compared the comp's slim partners marquee, minimal contact section, and three-cell footer against the live `components/partners-strip.tsx`, `components/contact.tsx`, `components/footer.tsx`. No actionable gap: Partners already matches the comp's intent (full-bleed marquee, no visible eyebrow, per owner requests 2026-07-08/07-11); Contact's comp-only two-card partner/artist outreach layout was already intentionally removed and consolidated into one form (owner change-set 2026-07-02), and its `rounded-xl` form-input radius vs. the comp's flat `4px` is left as-is (a deliberate touch-target/accessibility choice, not an oversight); Footer is a comprehensive post-comp redesign (owner request 2026-07-08 — 12-col brand/Company/newsletter grid, gold top hairline, back-to-top) that supersedes the comp's minimal three-cell stub entirely. No code changes.
- **Corporate type system, phase 12 — team card corporate polish; About/Press reviewed, no gap (owner redesign 2026-07-16, ported from the comp):** team member name (`components/team.tsx`, front card and bio overlay) moves to `font-condensed` at a larger size, matching the comp's Barlow Condensed treatment. The comp's short `LA`/`SF` location badges were not ported — this site's `data/team.yml` `location` field holds full descriptive strings, not short codes, so the comp's 9px badge size doesn't apply; filter-chip padding is unchanged since the `min-h-[44px]` touch-target floor already governs the real height. Reviewed About (comp's embedded press card was intentionally replaced by the live "Values" cards per the 2026-07-02 owner change — not a gap) and Press (no dedicated Press/News section exists in the comp — the live carousel is an original addition, not comp-constrained); no changes made to either. `docs/DESIGN.md` updated in the same commit.
- **Corporate type system, phase 11 — artists card corporate polish (owner redesign 2026-07-16, ported from the comp):** `components/artists.tsx` social-link icon chips shrink to `size-[26px]` circles with `size={14}` icons (matching the comp's proportions), the genre label gains `tracking-[0.08em]`, and the "Listen" label switches to a mono corporate-eyebrow treatment (was gold/semibold). Card shell, carousel mechanics, and embed lazy-loading untouched. `docs/DESIGN.md` updated in the same commit.
- **Corporate type system, phase 9 — events corner-bracket accent (owner redesign 2026-07-16, ported from the comp):** a small static `border-b border-l` L-mark (matching the hero's corner-bracket motif) sits at the bottom-left of each event card's poster region (`components/events.tsx`). Purely decorative — the fixed poster height, Embla carousel mechanics, and the uniform-card-shape (`mt-auto`) contract are untouched. `docs/DESIGN.md` and `tests/unit/event-card-shape.test.ts` updated in the same commit.
- **Corporate type system, phase 10 — services corporate numeral (owner redesign 2026-07-16, ported from the comp):** each services card renders a large, faint `01`–`06` numeral (`serviceNumeral()` in `components/services.tsx`, Barlow Condensed) behind the title, matching the comp's dossier-style card numbering. The comp's per-service thumbnail photo header was not ported (no image field in `services.yml`, same precedent as phase 2). `docs/DESIGN.md` and `tests/unit/look-and-feel.test.ts` updated in the same commit.
- **Corporate type system, phase 8 — nav (owner redesign 2026-07-16):** reviewed the comp's nav bar against `components/navigation.tsx` — no further visual gap found beyond the "Brands" link already added in phase 6 and the tagline treatment already landed in phase 4. The comp's nav is a simplified subset (no Team link, About/News merged) that doesn't match this site's richer information architecture; intentionally not narrowed to match.
- **Corporate type system, phase 18 — 404 eyebrow, found by a 40-agent parallel font-usage audit (owner redesign 2026-07-16):** the only genuine gap across every component/page file — `app/not-found.tsx`'s "404" label moves from the legacy inline eyebrow classes to `.lupfr-section-kicker`, the corporate mono eyebrow class every other section eyebrow was migrated to in phase 2 (this page was missed in that sweep). Two other agent findings were reviewed and rejected as false positives: About's Values-card numerals/titles (a pre-existing design element not driven by the comp, agent itself flagged as low-confidence) and the event status badge (`lib/events.ts`'s `EVENT_BADGE_*` classes) — the latter is explicitly documented in `docs/DESIGN.md` as intentionally matching section titles via `GoldShineText`, not the mono tag-pill pattern. `tests/unit/look-and-feel.test.ts` updated in the same commit.
- **Corporate type system, phase 17 — services thumbnail photography (owner redesign 2026-07-16, ported from the comp's `assets/services/*` — the one asset category phase 2 explicitly flagged as not integrated):** `data/services.yml` gains an optional `image` field, standardized to WebP through the existing `optimize-public-raster.mjs` pipeline and matched 1:1 by position to the comp's six services; `components/services.tsx` renders a `h-[120px]` full-bleed photo header above each card's padded content, matching the comp's thumbnail layout. `tests/unit/data-integrity.test.ts` updated in the same commit.
- **Corporate type system, phase 16 — section structure pass (owner redesign 2026-07-16, "more of the style/layout of the website that we had," ported from the comp):** section max-width widens sitewide from `max-w-7xl` (1280px) to `max-w-[1400px]` (nav, About, Artists, Brands, Contact, Events, Footer, Press, Services, Team), matching the comp's own `1400px` section wrappers. Services and Brands section headers move from centered to the comp's left-aligned title + right-aligned description row (matching Artists' existing pattern); Brands' grid widens to `xl:grid-cols-5` (was `lg:grid-cols-3`), matching the comp's 5-across desktop layout. Services and Brands desktop padding gains an `lg:py-24 xl:py-28` step toward the comp's more generous `120px` rhythm — mobile/tablet padding is unchanged everywhere (an earlier, unrelated owner directive tightened mobile vertical rhythm for time-to-usable). Hero, fonts, and the Artists carousel/data model are explicitly untouched per this session's direction. `docs/DESIGN.md` updated in the same commit.
- **Corporate type system, phase 15 — sitewide sharp-corner sweep (owner redesign 2026-07-16, "more boxy/corporate/minimal" direction, ported from the comp):** every remaining soft-rounded box/panel element sitewide (not already on the `rounded-sm`/`rounded-sm sm:rounded-md` card scale) moves to it: Contact form inputs/select/dropdown items, the phone-list popup inputs, Services' icon chip, Artists' embed frame, the event-detail hero frame, the (currently dormant) blog card, and the 404 logo focus ring. Pill CTAs and circular chips are unchanged — only content boxes go sharp, matching the comp. `docs/DESIGN.md` updated in the same commit.
- **Corporate type system, phase 14 — event card imageless-fallback stripe pattern (owner redesign 2026-07-16, ported from the comp):** the last gap found by a full re-read of the comp against every live section — an event row with no `image` (a future-proofing case; every current row has one) now renders the comp's diagonal `repeating-linear-gradient` stripe pattern instead of a flat gradient. `docs/DESIGN.md` updated in the same commit.
- **Corporate type system, phase 7 — hero corner readout (owner redesign 2026-07-16, ported from the comp):** `HeroCornerReadout` (`components/hero-shared.tsx`) adds the comp's decorative hero framing — a coordinates line and a location/year line (both via `BrandSlashText`, matching the brand-slash motif) pinned to the bottom corners, plus two small gold corner brackets. Static, no motion; desktop-only (`components/hero-desktop.tsx`) — the mobile hero (`hero-mobile-static.tsx`) is untouched to preserve its minimal-content performance budget. `docs/DESIGN.md` and `tests/unit/look-and-feel.test.ts` updated in the same commit.
- **Corporate type system, phase 6 — Our Brands section (owner redesign 2026-07-16, ported from the "LUPFR Website Restructure" comp):** new home section (`components/brands.tsx`, `data/brands.yml` → `lib/data/brands.ts`) rendering the five-card sub-brand portfolio — SEA//SIDE, HIGH//RISE, SOUND//CHECK, IN//SIDE, OUT//SIDE — that phase 2 had explicitly flagged as not integrated ("a 'Brands' section — doesn't exist on the live site"). Mounted eagerly at `#brands` directly under the partners strip and before Events, with a new **Brands** nav link; cards use the standard card shell (`rounded-sm sm:rounded-md`, no tilt), `BrandSlashText` titles set in `font-condensed` (first live use of the Barlow Condensed token reserved since phase 1), and a per-card `accent` hex scoped only to the tag pill/corner accents (card chrome stays on the shared gold `--accent`). Only SEA//SIDE links out (`https://seaside.la`); no `/brands` route or new photography was added. `docs/DESIGN.md`, `tests/unit/brands-list.test.ts`, `tests/components/brands.behavior.test.tsx`, `tests/unit/look-and-feel.test.ts`, and `tests/unit/home-performance.test.ts` updated in the same commit.
- **Corporate type system, phase 1 (owner redesign 2026-07-15, ported from "LUPFR Website Restructure" comp):** self-hosted `next/font/google` loading for Work Sans (body), Playfair Display (headings — now actually loaded instead of silently falling back to system serif), Barlow Condensed (new, reserved), and Space Mono (mono/eyebrows, replaces the never-loaded Geist Mono); new `.lupfr-section-kicker` corporate eyebrow class (mono, uppercase, wide-tracked) as a scoped exception to the sentence-case rule. `docs/DESIGN.md` and `tests/unit/look-and-feel.test.ts` updated.
- **Corporate type system, phase 5 — sharper card corners (owner redesign 2026-07-16, explicit owner sign-off to touch the locked squircle shape):** every card shell sitewide (`GoldCard`/Team, Featured Artists, Events, Services, Careers, About, Press, plus the footer newsletter panel, contact info cards, and the contact-list popup) moves from the `rounded-2xl`/`rounded-3xl` squircle to the sharper corporate `rounded-sm`/`rounded-md` scale, matching the comp; all paired top/bottom/glow radius classes updated together so no card shows mismatched corners (in particular, the Team bio-overlay panel in `components/team.tsx` now matches `GoldCard`'s new radius exactly, since it overlays the same card). `docs/DESIGN.md`, `tests/unit/event-card-shape.test.ts`, and `tests/components/gold-card.behavior.test.tsx` updated in the same commit; visually verified via screenshot across Events, Team, Artists, and Services.
- **Corporate type system, phase 4 — hero tagline (owner redesign 2026-07-16, explicit owner sign-off to touch the locked hero treatment):** the rotating hero tagline (e.g. "Redefining the Music Experience") switches from `font-sans tracking-tight` to `font-mono uppercase tracking-wide` in both `components/hero-desktop.tsx` and `components/hero-mobile-static.tsx`, matching the comp; the metallic gold shine effect itself is untouched. `docs/DESIGN.md` and `tests/unit/look-and-feel.test.ts` updated in the same commit.
- **Corporate type system, phase 3 — brand-slash divider (owner redesign 2026-07-16):** new `components/brand-slash-text.tsx` renders every "//" in a title as a skewed gold accent (`.lupfr-brand-slash`), matching the comp's brand-divider motif (e.g. "SEA//SIDE"); wired into event card titles (`components/events.tsx`) and the event detail page (`app/events/[slug]/page.tsx`) — a no-op for titles without "//". TDD: `tests/components/brand-slash-text.behavior.test.tsx` and `tests/unit/look-and-feel.test.ts` added/updated first.
- **Corporate type system, phase 2 — asset refresh + eyebrow rollout (owner redesign 2026-07-15):** refreshed artist (baum, fromclay, hlwa, thatfranco, zusebi), team (all 5), event poster (7 events), and corporate-partner (city_cruises, mutuals, umbrella) photos from the redesign source, matched 1:1 by existing name/slug through the existing WebP asset-prep pipeline. Applied `.lupfr-section-kicker` to every existing section eyebrow (Services, Team, About, Featured Artists, Careers, Press, Contact) in place of the old inline `text-gold-accent tracking-tight text-sm` treatment — copy unchanged. Footer eyebrow, partners strip (no section text), nav, CTAs, and form labels untouched. Not integrated: 3 unrostered artist photos (alex-rayne, malek, midfield-avenue — need bio/genre/socials), hero video, Services section photography (no existing image field), a "Brands" section (doesn't exist on the live site), and ambiguous/future-event gallery photos. `docs/DESIGN.md` and `tests/unit/look-and-feel.test.ts` updated.
- **Real mobile performance regression gates (measurement-only):** `scripts/verify-bundle-budget.mjs` (`bun run _verify:bundle-budget`) enforces a First Load JS byte budget for `/` from the build manifest during the build-check phase (including Vercel); `scripts/verify-mobile-perf.mjs` (`bun run _verify:mobile-perf`) measures LCP/FCP/long-task blocking/same-origin transfer under Playwright mobile emulation (best-of-N; skipped with `LUPFR_SKIP_BROWSER_CHECK=1`). Budgets live in `tests/performance/mobile-budgets.json`. These gates measure only — they do not change site look/feel or functionality. Wiring locked by `tests/unit/test-suite-gate.test.ts`; `docs/TESTING.md` updated.
- **Featured Spotify tracks for Auguste and Devvy Dub:** added the owner-provided Spotify track embeds to their Featured Artists cards via `data/artists.yml`; generated artist data and the artist data contract test now lock both URLs.
- **Owner change-set 2026-07-06 (Will's email):** two upcoming events in `data/events.yml` — **Devvy Dub Live** (July 24, 2026, 8–10:30 PM, ERIA Sausalito, Eventbrite ticket link) and **SEA//SIDE 001** (August 1, 2026, 7–10 PM, Long Beach Harbor, links to https://seaside.la) with flyers in `public/events/`; four artists in `data/artists.yml` — **Auguste** (Afro House, list position 1), **Devvy Dub** (Alternative Rock, position 2), **Nick Rosen** (House), **Admiral** (Afro House), portraits in `public/artists/`. Contract tests (`tests/unit/artists-list.test.ts`, `tests/unit/events-list.test.ts`) updated for the new order.
- **On-disk asset guardrails for artist + team portraits:** `tests/unit/data-integrity.test.ts` now asserts every featured-artist `image` and every team member `image` resolves to a real file under `public/` (mirrors the existing event-hero / gallery / partner checks), so a renamed or dropped owner portrait (e.g. Auguste, Devvy Dub, Nick Rosen, Admiral, Cianna) fails CI instead of shipping a broken image. `docs/TESTING.md` updated.

### Changed

- **Light-mode contrast correction (owner request 2026-07-17):** light mode moves from an overbright/faded full-white stage to a restrained neutral page (`#f0f0ed`), soft-white cards (`#fafaf8`), near-black ink (`#151515`), stronger borders/muted text, a lighter hero wash, and more visible Brands photography. Dark remains the explicit default and its palette is unchanged.

- **Swipe-first events + focused home artist roster (owner request 2026-07-17):** Upcoming/Past event rows keep their existing Embla drag/swipe and keyboard navigation but drop the small dot controls. The home Featured Artists section now shows only the first six ordered cards in an aligned three-column desktop grid; **View all artists →** opens the unchanged complete directory. A responsive all-name roster sits beneath the cards, with those six featured names rendered in the existing metallic gold and all remaining names in muted text — no bright-yellow filled chips. TDD source contracts cover the six-card slice, complete roster/directory, metallic featured treatment, aligned grid, and swipe-without-dots event behavior; DESIGN/ARCHITECTURE updated in the same change.

- **Featured Artists identity alignment (owner request 2026-07-17):** moved the GasMoney and trillbot genre, bio, Spotify profile, and featured-track player data onto their corrected display names while preserving both image paths and card positions.

- **Featured Artists label correction (owner request 2026-07-17):** swapped the GasMoney and trillbot display names so each existing artist image carries the correct label; images and all other artist data are unchanged.

- **Home event cards' no-poster tile now says "Coming soon" (owner request 2026-07-17):** the striped placeholder in the home Upcoming/Past carousels (`components/events.tsx`) previously rendered stripes alone; it now carries the same centered mono-uppercase "Coming soon" label as the `/events` directory cards and the event-detail hero. `tests/unit/look-and-feel.test.ts` locks the label; `docs/DESIGN.md` updated in the same change.
- **Reels recap block retired from the home Events section (owner request 2026-07-17):** removed the visible “Reels,” “Watch on Instagram,” and “View all reels” UI for now. The existing `data/reels.yml`, generated JSON, typed loader, and Instagram Reels link remain archived for a quick future restore. Upcoming/Past event cards keep the supplied restructure comp's exact 300px desktop width, square posters, 22px body padding, and 26px condensed titles.
- **Corporate type system, phase 30 — Brands photo fade (owner request 2026-07-17):** the five home Brands photos now render at the restructure comp's exact desktop teaser opacity (`0.16`) over the normal card surface; the added `black/55→85%` gradient scrim was removed so the photography has the requested faded/washed appearance. `docs/DESIGN.md` and `tests/unit/look-and-feel.test.ts` updated in the same change.
- **Events now matches the approved restructure comp:** the home header uses the mono `UPCOMING` kicker, condensed foreground `EVENTS` title, and an in-page “View all events →” archive link; Upcoming/Past cards now use the comp’s compact 300px square-poster format with relative-date + city pills, mono brand/date row, condensed uppercase title, gold subtitle, and bottom-pinned detail CTA. Optional `brandTag` / `city` presentation metadata was added to the three upcoming YAML rows; sorting, detail routes, Past, mobile behavior, and shared carousel mechanics remain intact. `docs/DESIGN.md` and `docs/ARCHITECTURE.md` updated.
- **Every corporate-partner logo now links to its destination:** Soundcheck, the only non-linked row, now points to its canonical card in LUPFR's Brands section (`https://lupfr.com/#brands`) while the LUPFR-owned podcast awaits a standalone site. The partner data contract and unit coverage now require an HTTPS URL for every row so future logos cannot silently render as non-clickable chips. `docs/DESIGN.md` and `docs/TESTING.md` updated.
- **Owner change-set 2026-07-11:** (1) removed the "Corporate partners" eyebrow text above the logo marquee (`components/partners-strip.tsx`; section keeps its `aria-label`, marquee now fades up on scroll via `ScrollReveal`); (2) re-encoded the desktop hero video in place (libx264 CRF 30, audio stripped, `+faststart`): ~1.46 MB vs ~2.96 MB at SSIM ≈ 0.97, with a ≤1.6 MB byte budget added to `tests/unit/hero-video-performance.test.ts`; (3) uniform card shape in the Upcoming/Past event carousels AND the Featured Artists carousel (slides stretch via flex; artist card body absorbs the height difference between embed and embed-less cards) — carousel items now stretch (dropped the `h-full` that disabled flex `align-items: stretch`) and the meta grid pins to the card bottom (`mt-auto`), locked by `tests/unit/event-card-shape.test.ts`; (4) scroll-reveal entrance animations added to the remaining static sections (News/Press header + carousel, partners marquee, and the Featured Artists header/carousel/CTA via a desktop-only `ArtistsRevealShell` — mobile keeps the always-visible static tree because of the past mobile black-screen regression, and `tests/unit/home-performance.test.ts` now enforces that tighter invariant); (4b) the partners strip lost its tinted `bg-muted/40 border-y` band and now inherits the page background entirely; (5) light-mode polish — partner logo chips get full-strength borders + a soft shadow so they read on the near-white band, and the hero bottom wash now resolves to ~full background at the seam instead of leaving a murky translucent band. New `tests/components/partners-strip.behavior.test.tsx`; `docs/DESIGN.md` + `docs/ARCHITECTURE.md` updated in the same change.

- **Faster mobile time-to-content (2–5s usable goal):** defer artist featured-track iframes until in view, use static mobile card inners for Services/Events (Past+Reels gated by `useInView`), downscale oversized event/artist WebPs (≤1600px / ≤450KB; `scripts/optimize-public-raster.mjs check` enforces), delay photo-card shimmer ~200ms so fast loads never flash, apply mobile-only `content-visibility: auto` to below-fold home sections, and tighten `tests/performance/mobile-budgets.json` ceilings (LCP ≤5s). Docs updated.
- **Mobile first-load JS reduction:** defer Press only (`DeferredHomeSection` + dynamic; Artists stays eager), wrap the app in non-strict `LazyMotion`/`domAnimation` (`MotionProvider`) so initial-chunk sections (Events/Services/Artists/shared primitives/phone popup) use Framer’s lighter `m` API, gate Services infinite orbs + card-tilt springs off mobile, include `#news` in `DEFERRED_SECTION_IDS` for stable hash scroll, and delete unused `liquid-metal-canvas`. Docs + `home-performance` / hash-scroll / look-and-feel guardrails updated.
- **Restored the pre-ring gold button design (owner rollback 2026-07-08).** Removed the `metal-fx` visual-effect dependency and deleted the `LiquidGoldFx` wrappers from desktop nav CTAs, Artists **Submit Your Mix**, Events **View all reels**, **Team filter chips**, and Team / Featured-artist cards. These controls now use the existing CSS gold button/pill styling plus `GoldCard` / `ArtistCard` behavior again; desktop card tilt is restored while mobile still disables tilt through `useIsMobile()`. `docs/DESIGN.md` and the home performance guardrail were updated.
- **Team section is a fit-all centered row (owner request 2026-07-08).** The `lg:grid-cols-4` grid left a large empty bottom-right (5 members) and the owner wanted every member visible without manual horizontal scrolling. `components/team.tsx` now uses a `flex flex-wrap justify-center items-start` layout: phones stay 2-up, `sm`+ cards `grow` (capped `max-w-[300px]`), so all five sit in one centered row on desktop and sparse filters (Exec/SF) center instead of stranding left. `docs/DESIGN.md` Team section updated.
- **Tightened empty vertical space on Artists + Reels (owner request 2026-07-08).** Artists "Submit Your Mix" CTA gap reduced (`mt-12` → `mt-6`) in `components/artists.tsx`; the Events section bottom padding stepped down (`pb-20 sm:pb-24 md:pb-28 lg:pb-32` → `pb-10 sm:pb-12 md:pb-14 lg:pb-16`) and the Reels block padding trimmed (`pt-6 pb-4` → `pt-4 pb-0`) in `components/events.tsx` so the gap between the Reels block and the Services section is much smaller.
- **Retired the `dev.seaside.lupfr.com` staging host (owner decision 2026-07-08).** Staging for SEA // SIDE now lives at **`dev.seaside.la`** on the seaside project (already live, HTTP 200), so the legacy dev host is no longer needed. Dropped `dev.seaside.lupfr.com` from `SEASIDE_HOSTS` in `proxy.ts` and removed the unused `SEASIDE_DEV_HOST` constant from `lib/site.ts`. The prod redirect `seaside.lupfr.com` → `https://seaside.la/` and the `/seaside` route redirect are unchanged. `tests/unit/proxy-blog-host.test.ts` now asserts `dev.seaside.lupfr.com` is absent from `SEASIDE_HOSTS` and falls through instead of redirecting; `docs/DEPLOYMENT.md`, `docs/ARCHITECTURE.md`, `docs/TESTING.md` updated. **Vercel follow-up:** detach `dev.seaside.lupfr.com` from the lupfr project (Settings → Domains) so it no longer serves the main site.
- **Mobile hero loads a tiny purpose-built poster (owner request 2026-07-08 — mobile was 20-30s).** The phone/low-compute hero LCP was a `next/image` poster whose *source* is the 2560×3413 / ~620-721KB desktop poster; the on-demand optimizer had to decode + AVIF-re-encode that huge source at device width on first hit, blocking mobile paint. Added dedicated ~1080px mobile posters `public/hero/hero-poster-{dark,light}-mobile.webp` (<90KB each) and pointed `components/hero-mobile-static.tsx` at new `HERO_POSTER_{DARK,LIGHT}_MOBILE` constants in `components/hero-shared.tsx`. **Desktop/laptop is unchanged** — `components/hero-desktop.tsx` keeps the full posters + video. Guardrail added to `tests/unit/home-performance.test.ts`; `docs/DESIGN.md` Mobile-performance note updated.
- **Footer visual redesign (owner request 2026-07-08):** refreshed `components/footer.tsx` styling only — no content, links, form fields, or contract changes. Adds a gold gradient top hairline + soft corner ambience, a 12-col brand/Company/newsletter layout, gold **eyebrow** column headings (`.lupfr-heading-eyebrow`, sentence case), circular bordered icon chips for the email/location/phone contact rows, an underline-sweep hover on the Company links, the newsletter (name/email/phone → `/api/phone-list`) wrapped in a bordered `bg-secondary/40` panel with a full-width **Join the list** button, and a circular back-to-top control. `docs/DESIGN.md` Contact-layout note updated.
- **Team bio expands in-place, not below (owner request 2026-07-08):** tapping **More** on a team card in `components/team.tsx` no longer height-accordions the bio below the card (which made that one card taller than its row). The bio now reveals as an `AnimatePresence` overlay covering the card in the same footprint — a `bg-card/95` backdrop-blur panel (`absolute inset-0`, internal scroll for long bios) with the member's name/title and a **Back** pill. `docs/DESIGN.md` Team section updated.
- **Partners marquee is full-bleed (owner request 2026-07-08):** the auto-scrolling logo row in `components/partners-strip.tsx` moved outside the section's `max-w-7xl` container so it runs edge-to-edge across the viewport on desktop and mobile; the "Corporate partners" eyebrow stays centered in the contained wrapper. Structural lock added to `tests/unit/look-and-feel.test.ts`; `docs/DESIGN.md` Partners + Layout-containment sections updated.
- **Cianna's team card is complete:** `data/team.yml` row upgraded from the pending-portrait placeholder to **Cianna Foppoli — Marketing & Strategy Intern** with her delivered portrait (`public/images/team/cianna.webp`, 1000×800) and full owner-provided bio; LA+SF tags unchanged. The "Portrait coming soon" placeholder branch in `components/team.tsx` stays covered by a mocked imageless fixture in `tests/components/team-section.behavior.test.tsx` (`tests/unit/data-lists.test.ts` updated).

### Fixed

- **Brand detail page: tag pill no longer jammed against the "All Brands" back link (owner screenshot 2026-07-17).** On `/brands/<key>` both the back link and the tag-pill kicker were `inline-flex`, so they rendered on one text line with zero gap (the link's `mb-8` never applied). The pill is now block-level (`flex w-fit`), putting it on its own row below the back link like the sibling `/services/<slug>` layout. Locked by a new source assertion in `tests/unit/look-and-feel.test.ts`.
- **CSS build crash from Tailwind scanning stray `NEXT_DIST_DIR` outputs:** a build failed with `Parsing CSS source code failed … Unexpected token Delim('%')` on a garbage safe-area `bottom` utility (the cookie-consent safe-area class with mangled "percent + ellipsis" bytes inside its `env()`). Root cause: ad-hoc isolated build dirs (e.g. `.next-spacing/`, `.next-visual/` from agent verification runs) were not gitignored, so Tailwind v4's automatic content scan crawled their contents and lifted a truncated/corrupted copy of the class as a utility candidate. `.gitignore` now uses a single `.next*/` glob (replacing the piecemeal `.next/`, `.next-ci/`, `.next-events/`, `.next-preview-*/` entries) so every present and future isolated dist dir is excluded from both git and Tailwind's scanner. Caution for docs/spec authors: Tailwind also scans `docs/*.md` — never quote a malformed class in literal bracketed utility syntax, or the scanner will emit it as real CSS.
- **Flaky `dev` deploy, round 2: raised the global Vitest timeout for Vercel's build machine.** After fixing the `groupGalleryByDateISO` flake (below), the next `dev` push failed on a *different*, unrelated test — `tests/integration/newsletter-route.test.ts`'s first case timed out at Vitest's 5000ms default while cold-importing the route module, on a build where cumulative transform/setup time was 150s+ (vs <1s locally) — clear evidence of transient machine contention on Vercel's shared 2-core/8GB Preview build machine, not a code bug. Raised `testTimeout`/`hookTimeout` to 20000ms in `vitest.config.ts` (systemic fix so future tests don't flake the same way) instead of patching tests one at a time. `docs/TESTING.md` updated in the same commit.
- **Flaky `dev` deploy: `groupGalleryByDateISO` performance guardrail could fail on Vercel's build machine.** `tests/performance/data-layer-budgets.test.ts`'s "groupGalleryByDateISO over full GALLERY_PHOTOS" case was the one test in the file using a single-sample timing (`bulkElapsedMs`) instead of the best-of-2 helper (`bestBulkElapsedMs`) its siblings use — it flaked at ~240ms against a 140ms ceiling on Vercel's shared 2-core/8GB build machine (unrelated to any app code; passed consistently and comfortably on local/dev hardware every time). Switched to `bestBulkElapsedMs(2, ...)` and raised the ceiling to 300ms. `docs/TESTING.md` updated in the same commit. This was the actual cause of the `dev` deploy erroring after the corporate-redesign phase 6-13 push — not a regression in that work.
- **Navbar section navigation no longer "takes forever to scroll".** Clicking a nav link to a deferred section (`#about`/`#team`/`#contact`) used to mount only the target, leaving the deferred sections above it as wrong-height placeholders — so the target's scroll position drifted while chunks loaded and a ladder of blind `setTimeout` realign retries (0–2200 ms in `home-page.tsx` plus 120/360 ms in `navigation.tsx`) chased the moving target for ~2s. New `lib/hash-scroll.ts` centralizes the behavior: `deferredSectionShouldMountForHash` mounts every deferred section at or above the target so it's positioned against real heights, and `realignHashTargetUntilStable` re-aligns across animation frames only until the position stops drifting (then stops — also on hash change away, user scroll, or a hard time cap). `components/navigation.tsx` drops its blind retry timers. Covered by `tests/unit/hash-scroll.test.ts`, `tests/components/deferred-home-section.behavior.test.tsx`, an updated `tests/unit/home-performance.test.ts` guardrail, and a new Playwright gate `scripts/verify-nav-scroll.mjs` (chunk-throttled, asserts each section lands flush under the header and settles fast).

## [1.20.0] - 2026-07-05

### Added

- **Instagram Reels block in Events (`data/reels.yml` → `lib/data/reels.ts` → `components/events.tsx`):** owner-requested recap-video section — one link card per reel row plus a "View all reels" pill to the new `LINKS.instagramReels`; cards link out to Instagram (no embedded third-party players). Contract locked by `tests/unit/reels-list.test.ts`.
- **Per-event photo gallery on `/events/[slug]`:** new `getGalleryPhotosByDateISO` in `lib/data/gallery.ts` feeds an **Event gallery** `GalleryPhotoGrid` (2-up phones / 3-up desktop) under the event description; covered by `tests/unit/event-gallery.test.ts` and the reworked `tests/behavior/gallery-home-journey.test.ts` URL contract (photo pages return to `/gallery`).
- **Team filter boxes (LA / SF / Exec):** `data/team.yml` rows carry `teams: [...]`; `components/team.tsx` renders All/LA/SF/Exec pill filters (`aria-pressed`), phones show 2 cards per row, and **Cianna** joins LA+SF with a "Portrait coming soon" placeholder until her photo arrives. Covered by `tests/components/team-section.behavior.test.tsx` + `tests/unit/data-lists.test.ts`.
- **SEA // SIDE interaction tests:** access/partner modal journey (submit success/failure, Close/Escape), edition carousel next/prev/dots/spec rows, artist profile swap, and menu-link close — restores the global coverage gate the prior seaside commit left red. Plus new `tests/components/gold-shine-text.behavior.test.tsx` and `tests/components/gold-card.behavior.test.tsx` shell contracts.
- **SEA // SIDE motion/mobile regression tests (`tests/components/seaside-landing.behavior.test.tsx`):** RTL locks for the hero h1 staying real `SEA // SIDE` text under the word-stagger reveal, the perpetual Ken-Burns zoom + reduced-motion opt-out in the scoped CSS, the three uniform top-anchored `.ss-partner-logo` slots, and the burger-nav `aria` wiring with ≥5 section anchor links. `docs/TESTING.md` inventory updated.

### Changed

- **Front page (owner email 2026-07-02):** the corporate partners marquee moved out of Services into a slim `components/partners-strip.tsx` strip directly under the hero (eyebrow line only, no big header), and the stats/metrics section is removed (`components/reviews.tsx` archived as `_deprecated/reviews-stats.tsx`).
- **Events on desktop:** card image heights cap at `md:h-[260px] lg:h-[280px]` and the carousel basis narrows to `lg:basis-[min(480px,33%)]` (≈3 cards visible) so event flyers are viewable without dominating the screen; phone sizing unchanged.
- **Gallery restructure:** the home `#gallery` carousel and album sections are retired (archived under `_deprecated/`), Gallery is removed from the nav and footer, and photos are reached from each event's page; `/gallery` and `/gallery/p/[id]` stay live so shared URLs keep working.
- **About:** founder portrait removed (his face lives in the Team section); the section is condensed around the story copy and closes with "Will Lupfer — CEO & Founder of LUPFR Entertainment" + the office address at the bottom.
- **Contact / footer:** the "Ready to elevate your event?" card is gone — its copy now sits under the "Let's Create Something" heading with the response-time pill — and the email / location / phone info cards moved to the footer brand column (`ProtectedPhone` extracted to `components/protected-phone.tsx`); the form and contact-list card sit in one centered column. Docs: `docs/DESIGN.md`, `docs/ARCHITECTURE.md`, `docs/TESTING.md` updated in the same change.
- **SEA // SIDE animation polish + logo alignment (`components/seaside/seaside-landing.tsx`):** the hero Ken-Burns zoom is restored to a clearly visible perpetual breathe (`ss-kb` now `scale 1→1.22` over 34s, infinite alternate, `will-change: transform` — was an imperceptible 1.05→1.18); new `AnimatedText` helper gives the hero h1 and every big Anton headline (Concept statement, Editions, Featured Artists, Partners, The Team, About, footer "The water is calling") a per-word blur/rise stagger reveal (words remain real text nodes, so wrapping and screen readers are unaffected; reduced motion collapses to a plain fade); partner/sponsor cards get a uniform 52px `.ss-partner-logo` slot so the LUPFR, Vybes, and Link Studios marks share one baseline (Link Studios uses a bordered "LS" monogram until it has a logo) and all three card headings now share one `clamp(26px,3vw,40px)` size. `docs/DESIGN.md` updated.
- **SEA // SIDE motion + mobile pass (`components/seaside/seaside-landing.tsx`):** the hand-rolled IntersectionObserver `Reveal` is reimplemented on **framer-motion** (`LazyMotion` + `domAnimation` + `m.*` to keep the client bundle lean), adding staggered list reveals (concept cards, team, partner cards), a hero scroll parallax + staggered entrance with `scaleX` divider draw-ins, `AnimatePresence` crossfades for the editions image and featured-artist profile swaps, animated modal enter/exit, and shared hover/tap CTA micro-interactions — every effect gated on `useReducedMotion`. Mobile fixes: hamburger nav below 768px (inline links/CTA replaced by an animated dropdown with ≥44px tap targets), hero `100svh` + h1/tagline overflow fixes at 320–375px, `scrollMarginTop` 84 for anchor jumps, edition panels collapse to content height under 640px with an `auto-fit` tier grid and ≥44px carousel-dot hit areas, lineup portrait fills the row when wrapped, and the modal gets 16px inputs (no iOS focus zoom), a `100dvh`-capped scrollable dialog, and a 44×44 close button. Copy, colors, fonts, section ids, and the `/api/phone-list` contract are unchanged. `docs/DESIGN.md` + `docs/ARCHITECTURE.md` updated.
## [1.19.3] - 2026-07-02

### Changed

- **Staging requires Vercel login again.** Vercel Authentication (Standard Protection) re-enabled on the project: `dev.seaside.lupfr.com` / `dev.lupfr.com` gate behind Vercel SSO; production domains (`lupfr.com`, `seaside.lupfr.com`) remain public. Reverts the 1.19.2 protection-off note in `docs/DEPLOYMENT.md`.

## [1.19.2] - 2026-07-02

### Changed

- **Seaside domains wired on Vercel; staging public.** `seaside.lupfr.com` and `dev.seaside.lupfr.com` are attached to the `lupfr` Vercel project, with the dev host pinned to the `dev` git branch (serves the current staging Preview). Vercel Authentication is disabled for the project, so `dev.seaside.lupfr.com` / `dev.lupfr.com` review no longer requires a Vercel login. `docs/DEPLOYMENT.md` records the status.

## [1.19.1] - 2026-07-02

### Fixed

- **Vercel pre-deploy gate now passes in the build sandbox.** The `1.19.0` gate failed the Vercel build: (1) `scripts/ci.sh` skip guards used `[[ … ]] || return`, which under `set -euo pipefail` returned the failed test's exit code (1) and aborted the run whenever a check was skipped (e.g. the browser crawl) — fixed to `return 0`; (2) the gate also booted `next start` and crawled it over HTTP (route smoke, asset crawl), which a Vercel **build** container cannot host. `vercel.json` `buildCommand` is now `LUPFR_SKIP_BROWSER_CHECK=1 LUPFR_SKIP_ROUTE_CHECK=1 LUPFR_SKIP_ASSET_CRAWL=1 bun run test && bun run _build` — the **sandbox-safe subset** (raster, smoke, lint, coverage, strict typecheck, production build, client-bundle scan) still blocks a deploy on any type error, lint failure, failing/uncovered test, or broken build. The server/browser crawls run in full via `bun run ship:dev`, pre-commit, and GitHub Actions. `docs/TESTING.md` + `docs/DEPLOYMENT.md` updated; `test-suite-gate` pins the new command.

### Added

- **`dev.seaside.lupfr.com` staging host.** Added to the seaside host list in `proxy.ts` (and `SEASIDE_DEV_HOST` in `lib/site.ts`) so a dev-branch Vercel Preview serves the microsite at its own staging subdomain (assign the domain to the `dev` branch in Vercel). Modal inputs also gained `aria-label`s. Proxy-rewrite test added for the dev host.

## [1.19.0] - 2026-07-02

### Added

- **SEA // SIDE microsite (`seaside.lupfr.com`):** new self-contained route `app/seaside` rendering `components/seaside/seaside-landing.tsx`, a faithful port of the SEASIDE brief design comp (hero, concept, editions carousel with the 001/002 slates, featured-artist roster, partners, team, about-LUPFR, and the request-access / partner modal). Served via a blog-style host rewrite in `proxy.ts` (`seaside.lupfr.com`, `seaside.localhost`); WebP assets under `public/seaside/`; Anton/Inter via `next/font`; the modal posts to the existing `/api/phone-list` with the intent folded into the name field; the LUPFR phone-list popup is suppressed on seaside hosts. `SEASIDE_HOST`/`SEASIDE_URL` added to `lib/site.ts` with unit + proxy-rewrite tests. Domain attach is a one-time dashboard step (docs/DEPLOYMENT.md).

### Changed

- **Vercel pre-deploy gate:** `vercel.json` `buildCommand` now runs the canonical full gate before the deploy build — `LUPFR_SKIP_BROWSER_CHECK=1 VERIFY_ROUTES_SKIP_EXTERNAL=1 bun run test && bun run _build`. A red gate fails the Vercel build itself; browser console crawl (no Chromium in the build image) and external-link QA (flaky egress) stay covered by the unskipped GitHub Actions run.

## [1.18.0] - 2026-07-02

### Changed

- **Team section restyle (owner request):** team cards now use the new reusable **`GoldCard`** shell (`components/gold-card.tsx`) — the Events-card squircle with gold accent border + gold wash on hover and pointer-only tilt — replacing the bordered/gold-glow card variant. Hover is deliberately pronounced: ±10° lean on team cards (`tiltMaxDeg` prop; events stay at 6°), slight scale lift, gold glow shadow, and a diagonal **gold glint sweep** across the card (`motion-reduce` hides it). Team portraits re-cropped to clean 1000×800 photos (baked gold frame + caption strip removed from the source artwork). Added the **Team** link to the primary nav (between About and Contact; locked in `tests/unit/look-and-feel.test.ts`).

- **Mobile load-time fix: production build switched from `next build --webpack` back to the default Turbopack build** (`package.json` `_build`). On Next 16.1.6 the webpack fallback ships Next devtools-overlay code in the root client JS: ~362 KB gzip (1.3 MB raw) on every page vs ~129 KB gzip (442 KB raw) with Turbopack — ~233 KB gzip less JavaScript to download/parse per page, the dominant mobile cost. The old Turbopack client-reference manifest gap on dynamic routes no longer reproduces: the full gate (dynamic-route smoke, asset crawl, Playwright console crawl against `next start`) passes on the Turbopack build. `EmptyPagesManifestPlugin` stays in `next.config.mjs` for explicit `--webpack` builds. Documented in `docs/DEPLOYMENT.md` / `docs/ARCHITECTURE.md`. **Verify dynamic routes (`/events/[slug]`, `/gallery/p/[id]`, APIs) on the Vercel Preview before promoting.**

### Added

- **629 website updates (owner request 2026-06-29).** Corporate partners: added Vybes, SEA//SIDE, City Cruises, and Mutuals, replaced the Soundcheck light/dark pair with the new single logo, and reordered to the requested sequence with Venn Social kept in place (`data/partners.yml`); Maison Noir is pending a logo asset. The partner row is now a **single-row auto-scrolling marquee** (`.partner-marquee` in `app/globals.css`; pauses on hover/focus, `prefers-reduced-motion` falls back to a static scrollable row). Added the **Our Team** home section (`data/team.yml` → `lib/data/team.ts` → `components/team.tsx`, mounted as `#team` after About) with Will Lupfer, Zac Brosky, Mateen Malekinejad, and Kylie Cortez, portraits in `public/images/team/`. Added the office address line (87 N Raymond, Pasadena, CA) to the About section. Added the **BOILER PARTY: MARINA** gallery album (`public/gallery/boiler_party_marina/`, 8 photos, `data/gallery.yml` ids 74–81). Documented in `docs/DESIGN.md` and `docs/ARCHITECTURE.md`.

- Added the June 8-15, 2026 blog run in `data/blog.yml`, featured the newest post on `/blog`, and refreshed blog article pages with large image-backed layouts and title-first back navigation.
- Disabled Next's experimental global 404 manifest path after production `next start` returned 500 for synthetic missing routes; unmatched URLs now rely on the self-contained `app/not-found.tsx` route while keeping the same public 404 behavior.
- Gated **production promotion on the canonical full test suite**: `bun run promote:prod` now refuses to run unless local HEAD is the exact `origin/dev` commit being promoted, then re-runs `bun run test` against it before the typed confirmation and fast-forward push. Both deploy environments (staging Preview via `ship:dev`, production via `promote:prod`) now run the identical gate — including dynamic route/link QA, the asset crawl, and the browser console crawl — before any push. Contract-tested in `tests/unit/test-suite-gate.test.ts`; documented in `docs/DEPLOYMENT.md` and `docs/TESTING.md`.

- Added the **dynamic asset-crawl integration suite** (`tests/integration/asset-crawl.test.ts`, `bun run _verify:assets`): boots the built production server, asserts every known route (static + blog slugs + event slugs) returns 200, and crawls each page's `<img>`/`<source>`/`<link>`/`<a>` asset references for 404s. Wired into `ci.sh` (`run_asset_checks`) so it runs inside `bun run test`. Also added the **look-and-feel CSS contract suite** (`tests/unit/look-and-feel.test.ts`) guarding typography, the gold visual system, and hero/partner/navigation/footer/animation contracts. Documented in `docs/TESTING.md`.

- Added scroll-triggered **per-word text reveal** animations (`components/text-reveal.tsx`, `TextReveal`): section lead paragraphs in Services, Featured Artists, Contact, and the About story now rise + fade in word by word as they scroll into view (Framer Motion `whileInView`, once, opacity/transform only). Screen readers get the full sentence via `aria-label`; `prefers-reduced-motion` renders static text. Documented in `docs/DESIGN.md` (Scroll-driven motion).

### Changed

- Temporarily disabled public blog access without deleting blog source/content: `/blog`, `/blog/*`, and blog-host traffic now return 404, Blog is hidden from navigation/footer/sitemap/route crawls, and `BLOG_PUBLIC_ACCESS_ENABLED` documents the restoration switch.

- Rewrote every blog article as a specific first-person LUPFR operator story tied to named events/situations (ERIA week, Boiler Boat, Where's West?, GAS MONEY, Third Thursday's, BOILER PARTY, Shamrock & House, and packed-weekend debriefs) and added unit guardrails requiring named situations, first-person voice, and opinionated thesis markers.

- Expanded every blog article into a denser long-read version with additional LUPFR event-growth, operations, candidate-set, recap, and partner-proof detail; tightened the article title/prose typography for smaller, more readable long-form blog pages.

- Reworked all blog posts into long-form LUPFR event-operator essays focused on event growth, candidate-set thinking, guest flow, partner proof, artist-room fit, recap systems, and creative/business process; blog article bodies now support markdown-style headings, bullets, pull quotes, and source-note cards with thinner animated prose typography.

- Updated the public **Call us** phone number on the contact card to **(323) 366-9246** and replaced numeric visitor phone placeholders with neutral copy so stale-looking example numbers no longer appear on the site. Locked with a source-level visual contract in `tests/unit/look-and-feel.test.ts` and documented in `docs/DESIGN.md`.

- **Light-mode visual cleanup (owner request 2026-06-11).** Light theme now reads as clean pure white with pure-black ink instead of off-white/cream: surfaces forced to `oklch(1 0 0)` (neutral faint-gray `--muted: oklch(0.97 0 0)` for functional fills only), foreground/headings/sublines to pure black `oklch(0 0 0)` (white text kept only on gold CTAs / dark primary), borders made transparent (`--border: oklch(1 0 0 / 0)`), and the ambient gold background washes removed — flat white body, header veil with no gold radial, flat white stat tiles with no border/gold gradient. Light gold retuned to a deeper **bronzed luxe** family (`--gold: oklch(0.55 0.13 76)`, softened specular `oklch(0.85 0.05 86)`, `--btn-gold*` + Entertainment-line stops stepped down to match) — less bright/glary, still has shine, hue coherent with the dark white-gold. Dark mode untouched. Updated `app/globals.css`, `tests/unit/globals-gold-theme.test.ts`, `docs/DESIGN.md`, and `docs/TESTING.md` in the same change.

- **Toned down light-mode gold** by owner request: `:root` `--gold` family stepped from `oklch(0.64 0.17 82)` to `oklch(0.58 0.15 80)` (with matching `--gold-dark` / `--gold-bright` / `--gold-specular` and `--btn-gold*` reductions) so headings, metrics, and gold CTAs read as deeper polished metal on white instead of bright glare. Dark-mode white gold untouched. Lock test `tests/unit/globals-gold-theme.test.ts` and `docs/DESIGN.md` baseline updated in the same commit.
- **On-photo gallery text is now white in both themes**: home carousel slides, album-grid slides, and `/gallery` grid hover captions switched from theme tokens (`from-background`/`from-card` scrims, `text-foreground`) to a dark black-based scrim with white title/date/caption text, and `GalleryEventBreadcrumb` gained an `onMedia` tone — so light mode no longer washes photos white or renders dark text over them (e.g. “Boiler Boat” titles). On-surface breadcrumbs (lightbox, photo page) keep theme tokens.

### Fixed

- Fixed home hash scroll stability: deferred Gallery/About/Contact mounts now realign only when the current hash still targets that same section, preventing `/#events` visitors from being pulled back upward while scrolling down the page.

- Fixed three blog post `coverImage` paths in `data/blog.yml` (and regenerated `lib/data/generated/blog.json`) that pointed at non-existent files — now `boiler_party_marina.webp`, `boiler_boat.webp`, and `third_thursdays_operator_sf.webp`, all verified present under `public/events/`. New `tests/unit/data-integrity.test.ts` cases assert every blog cover image and partner dark-mode logo exists on disk, and new unit suites cover the blog list (`tests/unit/blog-list.test.ts`), partner normalization (`tests/unit/partners-list.test.ts`), and the blog-host proxy rewrite (`tests/unit/proxy-blog-host.test.ts`). Documented in `docs/TESTING.md`.

- Removed three dead external links that were failing `verify:routes`: the Where's West past event's `ticketLink`/`ticketLabel` (organizer deleted the eriaevents.co product page, HTTP 404) and the `url` on the Venn Social and Brixton Bar SF partner chips (both domains no longer resolve). The event detail page simply omits the gold ticket CTA. The two partner chips were then **re-linked to the brands' live domains** (verified HTTP 200): Venn Social → `https://vennsocial.co`, Brixton Bar SF → `https://thebrixtonsf.com`.

- Fixed the desktop nav bar's light/dark **theme switch overlapping the section links** (e.g. sitting on "Contact") at laptop widths: the header grid's right track floor changed from a fixed `19rem` to `max-content` (`components/navigation.tsx`), so the toggle + Schedule a call + Book an Event cluster always gets its real width and can no longer spill left over the link row (which grew when the Blog link was added). Documented in `docs/DESIGN.md` (Navigation).

### Added

- Added a complete **Blog** feature: `data/blog.yml` + generated JSON pipeline support, `lib/data/blog.ts`, `/blog` index and `/blog/[slug]` article pages with metadata, three authored starter posts from the last 2-3 months in Will's voice, nav/footer blog links, and sitemap inclusion for each article route.
- Added host-based blog routing in `proxy.ts`: `blog.localhost` and `blog.lupfr.com` now rewrite to `/blog` routes so `http://blog.localhost:3001/` serves the blog directly while preserving `_next`, API, and SEO asset paths.

- Added a **site-wide animated gold gradient background** (Framer-style motion): smooth layered gradients now flow behind the entire site with motion-safe desktop emphasis, while mobile and `prefers-reduced-motion` fall back to a softer/static baseline.
- Added **Drive-as-CMS gallery albums**: `/gallery` now renders live "Event albums" (photos **and videos**) from the public Google Drive folder `LUPFR GALLERY/website/<event_folder>/` via `lib/drive-gallery.ts` + `components/drive-gallery-albums.tsx`. Listings use Drive's anonymous `embeddedfolderview` (no API key); photos serve from Google's CDN (`lh3.googleusercontent.com`, new `next/image` remotePattern), videos embed Drive's `/preview` player. ISR-cached (1 h, `app/gallery/page.tsx` `revalidate`), so a non-technical operator can add/replace/remove media in Drive and the site inherits the change with **no commit or deploy** — and heavy media stays out of the repo (faster builds, CDN-hosted assets). Drive folders map to event titles via `DRIVE_FOLDER_TO_ALBUM_FOLDER` (added `boiler_party_marina: 2026-06-05` to `GALLERY_ALBUM_FOLDER_DEFAULT_DATES`); unmapped folders auto-render with humanized titles. Fetch/parse failure logs and hides the section (page degrades to the committed grid). Operator workflow in `docs/RUNBOOK.md` § "Gallery media via Google Drive"; parser/mapping covered by `tests/unit/drive-gallery.test.ts`.
- Added a cross-agent **docs-first / plan-first gate** so every AI agent (Claude Code, Codex, Cursor, Copilot) reads the owning `docs/*.md` spec and writes `tmp/plan.md` before any non-trivial edit: new repo-root `AGENTS.md` (canonical agent contract with code-to-docs map), `CLAUDE.md` (imports `AGENTS.md`), and `.cursor/rules/123-plan-before-acting.mdc` (always-on plan gate alongside the existing Lupfr Cursor rules, which are unchanged).
- Added a standalone **Careers** page at `/careers` (`app/careers/page.tsx`, `components/careers.tsx`), separate from the home page: data-driven open-role cards from `data/careers.yml` → `lib/data/careers.ts` (added `careers` to `scripts/generate-data.js`), each linking out to its LinkedIn job posting ("Apply on LinkedIn"), plus a "Life at LUPFR on LinkedIn" link and an empty-state message when no roles are open. Initial roles: Talent & Events Intern and Marketing & Strategy Intern (Los Angeles). Added `linkedin` + `linkedinLife` to `lib/links.ts`, a LinkedIn footer social link, a footer Company → Careers link, and data-integrity tests validating careers card content and LinkedIn job URLs. Sitemap picks up `/careers` automatically.

- Fixed the section-title second line (`.lupfr-heading-subline` via `--lupfr-heading-subline-fg`) being dark grey in **both** themes: the token now resolves to near-black ink in light mode (improved contrast, `oklch(0.21 0.018 263)`) and theme `foreground` white under `.dark` — applies globally to "Artists", "Partners", "Event pics", "Something", etc. Updated `docs/DESIGN.md` token description to match.
- Added a **News (Press / Editorials)** home-page section (`components/press.tsx`, section id `news`, nav link "News", gold-shine "News" heading) between Artists and Gallery: a carousel of clickable article cards (image, outlet badge, date, title, excerpt, "Read the article" link opening the source in a new tab), one card per slide on mobile, two per slide on desktop, matching the Artists section's gold-heading/tilt-card theme. Content is data-driven via `data/press.yml` → `lib/data/press.ts` (added `press` to `scripts/generate-data.js`). First entry: the San Francisco Post feature on LUPFR (image at `public/press/sf-post-lupfr-feature.webp`). Data-integrity tests verify press images exist under `public/` and article URLs are https.
- Added a smoke-first Vitest subset (`bun run _test:smoke`) that runs at the start of both `bun run test` and `bun run smoke`, and made Vitest workers configurable via `VITEST_MAX_WORKERS` with a measured `50%` default to fail faster without over-scheduling local machines.
- Simplified the public Bun workflow to one command per operator job: `bun run dev`, `bun run start`, `bun run test`, `bun run smoke`, `bun run ship:dev`, and `bun run promote:prod`. Internal build/lint/route/browser/image plumbing moved behind underscored scripts, while GitHub Actions, Vercel, and pre-commit now call `bun run test` directly.
- Added `tests/unit/test-suite-gate.test.ts` to lock the single-gate contract so `bun run test` keeps running coverage, gallery journey coverage, dynamic route/external-link QA, and browser runtime crawl.
- Updated the installed pre-commit hook source so commits run the canonical full test gate directly.
- Documented the repo-local documentation discipline in `docs/RUNBOOK.md`: behavior, deployment, testing, content, and public-surface changes must update the relevant `docs/` spec and `docs/CHANGELOG.md` before commit.
- Added `scripts/verify-console.mjs`: a headless **Chromium** (Playwright) smoke check that starts the production server, crawls every internal route reachable from `/`, and **fails the run on any browser `console.error`, uncaught page/runtime error (including React hydration #418/#423), or same-origin HTTP `>= 400` response** — the class of Next.js client errors the route crawl cannot see. Wired into `bun run test`; opt out locally with `LUPFR_SKIP_BROWSER_CHECK=1`.
- Swapped the Where's West featured-artist photo for a higher-resolution asset (`public/artists/wheres_west.webp`, 300x300 -> 609x621) and updated the pinned image-hash guard in `tests/unit/artists-list.test.ts`.
- Added dynamic external-link QA to the route crawl: while crawling internal routes, the production server crawl now collects every external `<a href>` (Instagram, TikTok, YouTube, Partiful, Google Calendar CTA) via `scripts/extract-external-links.mjs` and health-checks each with a browser UA and a tolerant policy (bot-block `401/403/405/429` = alive; only DNS/connection failure, `404/410`, or `5xx` fail; timeout = `WARN`). Runs in `bun run test`, `bun run smoke`, and on Vercel; opt out of just the outbound checks with `VERIFY_ROUTES_SKIP_EXTERNAL=1`. Added `tests/unit/extract-external-links.test.ts` covering the extractor contract.

- Added `bun run ship:dev` and `bun run promote:prod` scripts (`scripts/ship-dev.sh`, `scripts/promote-prod.sh`) implementing the documented branch-based staging flow: `ship:dev` fast-forward pushes the current commit to `dev` for a Vercel staging Preview, and `promote:prod` fast-forwards `main` to the validated `dev` commit (refuses on divergence, prints the promoted commits, requires a typed `yes`, never force-pushes). Resolves the drift where `docs/DEPLOYMENT.md` referenced these scripts before they existed.
- Added Soundcheck to Corporate Partners with an optimized WebP sponsor logo asset and non-linked partner rendering support.

### Added (Content — 2026-06-05)

- Added **GAS MONEY** event (June 20th, 2026, ERIA MARINA) with poster `public/events/gas_money.webp` via Claude Code AI-assisted content update; entry lives in `data/events.yml` id 12.
- Updated **Rhythm n Friends** poster (`public/events/rhythm_n_friends.webp`) with the revised artwork from the 604 Website Update zip.

### Fixed

- Removed the blank/deferred scroll gap between the home Events and Services sections by mounting `Services` eagerly after `Events`, and added a home-performance guardrail so future changes cannot reintroduce that placeholder delay.
- Reduced event-navigation latency: home `#events` now mounts eagerly instead of waiting on deferred-section observer mounting, and event-detail Partiful metadata fetches now use a short timeout plus request-scoped memoization so remote slowness cannot stall detail-page navigation.

- Cleaned up the light-mode visual hierarchy in the home Reviews metrics area: simplified the light page wash, switched the strip to near-white instead of muted-beige, refined stat card surfaces to cleaner glass-white layers, and softened indicator/hover contrast for a more cohesive modern baseline while keeping dark mode unchanged.
- Kept the home `#gallery` carousel (`components/gallery.tsx`) auto-advancing reliably after manual interaction by removing the fragile pointer-drag pause gate that could leave autoplay parked.
- Fixed a React hydration mismatch on the hero poster (`<img>` src/srcSet diffing `hero-poster-light.webp` vs `hero-poster-dark.webp`): `components/hero-mobile-static.tsx` and `components/hero-desktop.tsx` read next-themes `resolvedTheme` during the initial render, so a light-theme visitor's client picked the light poster while SSR had rendered dark. Both now use the shared `useHeroTheme()` in `components/hero-shared.tsx`, which resolves **dark** on the server and first client render (`useSyncExternalStore` hydration gate) and swaps to the stored theme after hydration — desktop hero video src uses the same gate so poster/video stay paired.
- Fixed inconsistent page margins on wide viewports (~1440–1600px): Tailwind v4's bare `container` caps at the raw breakpoint (1536px), so sections without an explicit width cap (About — Will's portrait hugged the left edge — Services, Artists, News, Reviews stats, Contact, Footer, and the nav bar's inner row) rendered nearly edge-to-edge while Events/Gallery/Careers stayed contained. All `container` wrappers now share the `max-w-7xl` (1280px) cap, so every section sits inside the same centered margins (`docs/DESIGN.md` § UI/UX records the rule).
- Reordered the Boiler Boat 003 gallery album so it leads with the full-deck crowd shot (`boiler_boat_003_09.webp`) instead of the posed group photo (`_01`, moved near the end with an accurate caption/alt); affects the home `#gallery` carousel lead slide and `/gallery` album order (`data/gallery.yml`).
- Made the home gallery carousel preload window directional (`components/gallery.tsx`): the carousel now decodes the **next 2** slides ahead of the deterministic forward autoplay (plus 1 behind for back-swipes) instead of a symmetric ±1 ring, so every rotation swap shows an already-decoded image with no shimmer while still never mounting all full-size photos at once.
- Kept the first slide of every album decoded with an eager fetch (`eagerDecode` on the album jump-bar targets) so clicking a jump-bar chip lands on an already-loaded photo instead of a skeleton shimmer.
- Removed lazy client-only loading for the home Featured Artists section and switched to a direct import so artists render deterministically on dev/laptop without waiting on a deferred chunk placeholder.
- Mounted the Featured Artists section eagerly on the home page (instead of deferring it) so laptop/desktop navigation cannot land on a blank placeholder before artist cards render.
- Simplified Featured Artists rendering for low-compute devices by forcing the lightweight mobile-style card view when compute/network constraints are detected, preventing heavy desktop effects from blocking artist visibility.
- Documented the local-development terminal rule in `docs/RUNBOOK.md`: run `bun run dev` in a foreground terminal only (no detached/background process), and clear any existing port-3000 listener before restart.
- Made Featured Artists use a static mobile grid with plain image cards and no mobile carousel/player embeds, fixed hash scrolling after the lazy mobile section mounts, and removed Operator SF from the featured artist lineup.
- Replaced the Rhythm n Friends event poster with the blue Rhythm N Friends artwork (`public/events/rhythm_n_friends.webp`).
- Restored the Where's West event poster (`public/events/wheres_west.webp`) to the original 1080x1350 event artwork while keeping the updated artist photo at `public/artists/wheres_west.webp`.
- Hardened the browser console crawl so intentionally blocked heavy media/font/image requests and aborted Next RSC prefetches do not fail the gate while same-origin HTTP errors and real request failures still do.
- Improved first-screen performance by removing Framer Motion from the fixed navigation and keeping the base schedule-call CTA free of motion-only imports; the navigation section spy now avoids redundant DOM scans during scroll.
- Documented the Vercel protected-preview SEO failure mode and made `bun run ship:dev` warn that Lighthouse indexing audits must use production, a Shareable Link, or the `x-vercel-protection-bypass` automation path.
- Delayed the first-visit cookie notice past the initial load window, disabled first-paint prefetches from visible nav/legal links, tightened mobile deferred-section mounting, and removed an unused Unsplash preconnect to improve Lighthouse mobile LCP and transfer.
- Kept the Playwright console crawl focused on runtime errors by fulfilling heavy image/media/font requests with empty `204` responses, skipping per-photo gallery routes, ignoring expected Next `_rsc` prefetch aborts, and treating target-close settle races as warnings, preventing large gallery crawls from exhausting or flaking the built Next server during `bun run test`.
- Enabled the self-contained global 404 document for synthetic missing-route checks so they do not hit Next.js client-reference-manifest instability for `/_not-found`.
- Hardened the Playwright browser crawl against long-lived media/analytics requests by waiting for DOM readiness plus a short settle window instead of blocking on `networkidle`.
- Moved Vercel Preview/Production builds back to the production build command (`bun run _build`) and made `bun run ship:dev` run the canonical full gate before pushing `dev`; GitHub Actions now installs Playwright Chromium before `bun run test` so the browser crawl works in CI.
- Bounded dynamic external-link QA by collapsing repeated gallery social-share endpoints to one live health check per share endpoint after route crawling, keeping `bun run test` comprehensive without hundreds of duplicate network checks.
- Tightened the desktop About section grid so the Will Lupfer portrait and value cards sit closer together.
- Kept the fallback pages-manifest Webpack plugin out of `next dev` so dev builds no longer crash with duplicate `pages-manifest.json` asset emissions.
- Isolated `bun run test` / `bun run smoke` build output under `.next-ci/`, made client-bundle verification follow `NEXT_DIST_DIR`, and added dev-cache preflight cleanup so `.next/dev/routes-manifest.json` ENOENT loops self-heal instead of recurring.
- Renamed the June 19 Rhythm event slug/title/description from the birthday-bash wording to `Rhythm n Friends` in source event data.
- Guarded homepage dynamic imports with a named-export resolver so missing deferred section exports fail with a clear error before React lazy can render `undefined`.
- Removed duplicate hero poster preloads, made the immediately visible Reviews section a normal import, and added route coverage for favicon folder fallbacks plus the legacy `/layout.css` stylesheet request to eliminate browser-console preload/404 noise.
- Removed build-time Google font fetching so production builds do not hang on remote font sockets.
- Hardened Next.js verification by making `build` run strict `typecheck` before `next build`, constraining build workers to avoid `.next` races, disabling the unstable experimental global-not-found build path, and cleaning up navigation/CSS diagnostics that surfaced as app-side warnings.
- Made `app/global-not-found.tsx` a self-contained static 404 document and kept global fallbacks off `next/link` so unmatched/error routes do not pull App Router client internals into document-level bundles.

## [1.0.8] - 2026-06-04

### Added

- Added Rhythm n Friends event details, ticket link, and optimized poster asset.
- Added Shamrock & House and Third Thursday's gallery albums with optimized WebP images.

### Changed

- Updated the fromclay + thatfranco event date format, ticket link, and poster asset.
- Hardened header tab links so same-page mobile hash navigation and subpage-to-home section links work reliably.
- Reduced mobile-only deferred section placeholder gaps so scrolling from Corporate Partners into Gallery feels continuous.

## [1.0.7] - 2026-06-01

### Changed

- Stabilized the hero `LUPFR` wordmark by replacing the animated clipped-text shine with a static metallic gradient and lighter shadowing to avoid video-overlay repaint flicker.

## [1.0.6] - 2026-06-01

### Changed

- Added the Yahsek Dropbox video folder to Boiler Boat 003 post-event content links.
- Updated the homepage hero tagline to `Redefining the Music Experience` and paused desktop hero video playback in favor of real event-photo poster media.
- Added June 19, 2026 FIFA World Cup Watch Party and July 25, 2026 Marina Music 002 events with optimized poster assets.
- Added fromclay and thatfranco to Featured Artists, keeping the requested six artists first and enabling carousel pagination for more than six artists.
- Reused the shared carousel primitive for Events and Featured Artists controls instead of section-specific dot navigation.
- Added Nazarian Law and Empress Yachts to Corporate Partners with optimized transparent logo assets.
- Updated the Schedule a call destination to the new Google Calendar booking link.
- Updated the FIFA World Cup Watch Party Partiful link and added reusable `ticketStatus: tbd` handling for disabled event CTAs.
- Renamed the July 25 Marina Music event display to `fromclay + thatfranco` and updated its time to 9 PM - 1 AM.
- Reduced homepage scroll flicker by removing fixed-background and backdrop-blur repaint triggers, making section reveals one-shot, and using static metallic shine by default.
- Enabled the desktop hero video with an optimized real ERIA Marina event loop and poster fallback.
- Added a post-build client-bundle verification gate to block the retired hero shine references that caused the production client crash.
- Hardened Vitest browser-storage setup so malformed local `localStorage` globals no longer break cookie/contact preference tests.
